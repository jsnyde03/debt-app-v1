{
  "S1P1-B1-OWNER": {
    "what": "pass-1 B1 — the whole app had two trust guards, both inline in money.tsx, while selectPlanState and progress.tsx made the same 'every balance cleared' claim with none; measured on one store, Money refused while Today asserted",
    "file": "apps/rn/src/store/trustSelectors.test.ts",
    "token": "Today must NOT reach the debt-free celebration"
  },
  "S1P1-B1-PROGRESS": {
    "what": "pass-1 B1's third screen — progress.tsx contained zero references to pendingDataRepairs and rendered a DEBT-FREE trophy shelf; the first fix then dropped the user into 'Add a debt', which was a second false statement",
    "file": "apps/rn/tests/e2e/progress-hero-journey.spec.ts",
    "token": "Progress does not hand out a trophy for balances it could not read"
  },
  "S1-BLOCKER-1": {
    "what": "Number('') is 0, so a blank balance was stamped `recovered`; Money's narrowed guard then rendered \"Every balance cleared\" over debts still owed, permanently",
    "file": "apps/rn/src/data/migrations.test.ts",
    "token": "blocker #1 — Number(\"\") is 0, so this used to read as recovered"
  },
  "S1-BLOCKER-1-E2E": {
    "what": "the user-facing half — every prior fixture seeded `balance: null`, the one member of the class that worked; measured: all 10 existing data-recovery tests stay GREEN with the blocker planted back",
    "file": "apps/rn/tests/e2e/data-recovery.spec.ts",
    "token": "a BLANK balance is unread money too"
  },
  "S1-ABSENT-REQUIRED": {
    "what": "repairMoneyFields skipped every undefined, so a row missing a REQUIRED money field reached the store as `undefined` with no repair recorded — in neither the active nor the paid-off list, and every total NaN",
    "file": "apps/rn/src/data/migrations.test.ts",
    "token": "REQUIRED_FLOOR"
  },
  "S1-M9-GUARDIAN": {
    "what": "isEmergencyFund was `goal.type === 'emergency'`, so the Guardian called a SECOND emergency-typed pot \"your emergency fund\" while Money labelled the same goal Savings; no fixture in the repo carried two emergency goals on this path",
    "file": "apps/rn/src/store/guardianSelectors.test.ts",
    "token": "it is NOT called the emergency fund"
  },
  "S1-M9-SHEET": {
    "what": "GoalSheet's Type selector offered 'Emergency fund' for a goal that cannot be one, so the sheet, Money and the Guardian gave three answers for one goal; the repo's only two-EF fixture asserted the CONTROLS, not the labels",
    "file": "apps/rn/tests/e2e/goal-pace-edit.spec.ts",
    "token": "a second emergency fund is called Savings in the sheet too"
  },
  "S1-SECRETS-EXEMPT": {
    "what": "[D72] lint:secrets fired 4 times on the audit report that documents its own plant, so lint:rn was red on every committed tree since the fix; exemptions are now keyed on a SHA-256 of the matched value, with a self-ratcheting cap and stale entries red ⛔ S1.9.4 after-scan — re-pointed at the line that would have to change. The auditor named seven bare-identifier guards; measured across all 23 non-test entries there were six more of the same shape.",
    "file": "scripts/check-committed-secrets.ts",
    "token": "if (exemptions.length !== MAX_EXEMPT)"
  },
  "REVERIFY4-1": {
    "what": "the freshness record was fingerprinted at the END of validate:release:rn, so a mid-run edit was recorded as tested",
    "file": "scripts/write-gate-status.ts",
    "token": "SOURCE MOVED WHILE THE SUITES WERE RUNNING"
  },
  "REVERIFY4-2": {
    "what": "lint:secrets took the file list from git and the content from the working tree, so it passed over a HEAD holding a credential",
    "file": "scripts/check-committed-secrets.ts",
    "token": "cat-file"
  },
  "REVERIFY4-3": {
    "what": "13 stale duplicate-copy baseline entries were standing permissions to re-duplicate",
    "file": "scripts/strings-inventory.ts",
    "token": "no longer duplicate"
  },
  "REVERIFY4-4": {
    "what": "check-type-scale treated any allowFontScaling as a clamp, including ={true}",
    "file": "scripts/check-type-scale.ts",
    "token": "function isClamp"
  },
  "REVERIFY4-5": {
    "what": "preflight-native-lane's four flow-ordering assertions sat inside a fail-open if (iphoneList.length) ⛔ S1.9.4 after-scan — re-pointed at the line that would have to change. The auditor named seven bare-identifier guards; measured across all 23 non-test entries there were six more of the same shape.",
    "file": "scripts/preflight-native-lane.ts",
    "token": "assertions ran; ${MIN_CHECKS} are expected"
  },
  "GAP-1": {
    "what": "invariant 9's reachability — deleting the goals[1] loop in corpus.ts restored the original defect with the repo green",
    "file": "apps/rn/src/data/migrationAudit/audit.test.ts",
    "token": "invariant ⑨ is unreachable"
  },
  "GAP-16": {
    "what": "no planted-input harness proved any gate fails CLOSED — all 15 of S0's majors were gates failing open ⛔ S1.9.4 after-scan — re-pointed at the line that would have to change. The auditor named seven bare-identifier guards; measured across all 23 non-test entries there were six more of the same shape.",
    "file": "scripts/test-gate-plants.ts",
    "token": "if (SCENARIOS.length < MIN_SCENARIOS)"
  },
  "D69-INVENTORY": {
    "what": "\"first look\" was the auditor's own claim; [D69] needs it to be a lookup ⛔ S1.9.4 [pass-2 B-1] — the token now names THE LINE THAT WOULD HAVE TO CHANGE, not an identifier the un-fix leaves in place; the behavioural proof is the test:gate-plants scenario / self-check.",
    "file": "scripts/surface-coverage.ts",
    "token": "if (missing.length || stale.length)"
  },
  "GUARDED-1": {
    "what": "the untokenised caps red on a prose rescue ⛔ S1.9.4 after-scan — re-pointed at the line that would have to change. The auditor named seven bare-identifier guards; measured across all 23 non-test entries there were six more of the same shape. ⚡ S1.9.7 [B-2] then changed that very line from `>` to `!==` and this token REDDED, which is the re-pointing doing its job — a token on the bare identifier would have said nothing either way.",
    "file": "scripts/check-audit-closure.ts",
    "token": "if (p68Untokenised.length !== MAX_UNTOKENISED.p68)"
  },
  "GUARDED-3": {
    "what": "selfCheck proves the invariants fire, the verdict throws, and that run() still CALLS it",
    "file": "apps/rn/src/data/migrationAudit/audit.test.ts",
    "token": "export function selfCheck"
  },
  "GUARDED-4": {
    "what": "HOSTILE_FLOOR asserts the hostile corpus still reaches the migration logic",
    "file": "apps/rn/src/data/migrationAudit/hostile.test.ts",
    "token": "HOSTILE_FLOOR"
  },
  "GUARDED-5": {
    "what": "the branch invariant 9 judges — a priority goal's pace reading as uncapped ⛔ S1.9.4 after-scan — re-pointed at the line that would have to change. The auditor named seven bare-identifier guards; measured across all 23 non-test entries there were six more of the same shape.",
    "file": "apps/rn/src/data/migrationAudit/invariants.ts",
    "token": "  priorityGoalIsCapped,"
  },
  "GAP-2": {
    "what": "deleting any invariant from INVARIANTS is silent; INVARIANTS.length is printed and asserted nowhere",
    "unguarded": "S0.13 remaining — one poisoned outcome per invariant, plus an INVARIANTS.length floor. See S0-GUARDS-4.md"
  },
  "GAP-3": {
    "what": "moneyKeepsItsType's GOAL_MONEY_FIELDS catch nothing on the live corpus, so deleting them is silent",
    "unguarded": "S0.13 remaining — folds into GAP-2's harness. See S0-GUARDS-4.md"
  },
  "GAP-4": {
    "what": "removing stripMarkdownCode mints 4 fabricated closure ids and lint:closure stays green",
    "unguarded": "S0.13 remaining — a fixture carrying the token in every markdown spelling, plus a downward-only assertion on explicit.size. See S0-GUARDS-4.md"
  },
  "GAP-5": {
    "what": "nothing reads the closure gate's own stderr, so the pass-4 indent fix can be reverted unobserved",
    "unguarded": "S0.13 remaining — run both remediation strings through stripMarkdownCode and assert both blank. See S0-GUARDS-4.md"
  },
  "GAP-6": {
    "what": "MAX_UNTOKENISED's downward-only rule is stated in a docstring and enforced by nothing",
    "unguarded": "S0.13 remaining — fail when the count is BELOW the cap, making it self-ratcheting. See S0-GUARDS-4.md"
  },
  "GAP-7": {
    "what": "HOSTILE_FLOOR can be LOWERED to absorb a fixture that stopped opening",
    "unguarded": "S0.13 remaining — assert HOSTILE_FLOOR === CASES.length. See S0-GUARDS-4.md"
  },
  "GAP-8": {
    "what": "seven of ten strip-using gates report green while reading nothing when the stripper is blinded",
    "unguarded": "S0.13 remaining — a per-gate hit floor, downward-only, as check-destructive-writes already does. See S0-GUARDS-4.md"
  },
  "GAP-9": {
    "what": "scripts/lib/stripCode.ts has no test at all",
    "unguarded": "S0.13 remaining — partly covered by GAP-16's harness; a direct unit test over the modelled constructs is still owed. See S0-GUARDS-4.md"
  },
  "GAP-10": {
    "what": "check-month-arithmetic's five banned spellings and constructorOverflow have no test",
    "unguarded": "S0.13 remaining — GAP-16 now plants one spelling; the other four and the exempt idioms are owed. See S0-GUARDS-4.md"
  },
  "GAP-11": {
    "what": "reverting check-destructive-writes' CALL to the pre-M11 call shape leaves the gate green",
    "unguarded": "S0.13 remaining — fixtures for the four importStore spellings its docstring tabulates. See S0-GUARDS-4.md"
  },
  "GAP-12": {
    "what": "the CRLF sweep is invisible to an LF-only ubuntu-latest CI",
    "unguarded": "S0.13 remaining — a CRLF fixture committed with .gitattributes -text. See S0-GUARDS-4.md"
  },
  "GAP-13": {
    "what": "gateSources ROOTS can lose a root without anything noticing",
    "unguarded": "S0.13 remaining — a file-count floor on the fingerprint. See S0-GUARDS-4.md"
  },
  "GAP-14": {
    "what": "lint:gate-freshness is in no chain — not run-gates.ts, not validate:release:rn, not CI",
    "unguarded": "S0.13 remaining — deliberate per its own docstring (a freshness check inside the thing that establishes freshness deadlocks); the open question is whether a POST-run check belongs in CI. See S0-GUARDS-4.md"
  },
  "GAP-15": {
    "what": "strings-inventory's origin-label self-check has no test of its own",
    "unguarded": "S0.13 remaining — subsumed by GAP-16's harness once a scenario is added. See S0-GUARDS-4.md"
  },
  "GAP-17": {
    "what": "the apostrophe and webkit-flex baselines can be regenerated WIDER to make a red gate green",
    "unguarded": "S0.13 remaining — duplicate-copy is fixed (REVERIFY4-3); the other two need the same stale-drift report. See S0-GUARDS-4.md"
  },
  "GAP-18": {
    "what": "a raw NUL in the ledger makes it a binary file to rg, so the closure gate's corpus stops being greppable",
    "unguarded": "S0.13 remaining — assert the two SOURCES contain no NUL. See S0-GUARDS-4.md"
  },
  "S1P1-B5-COUNT": {
    "what": "pass-1 B5 — index.tsx emptied `unfunded` for a premium user with a recovery plan, and `outstanding` was computed from the emptied array, so Today rendered \"You're caught up for this paycheck.\" in success green over unpaid bills; free was the working control",
    "file": "apps/rn/tests/e2e/no-bills-branch.spec.ts",
    "token": "PREMIUM in a shortfall is NOT told they are caught up"
  },
  "S1P1-B5-TIER": {
    "what": "pass-1 B5 — the free control. `recovery` is `isPremium ? … : null`, so the tier decided what the user was told they owed; the two tiers must agree on an identical store",
    "file": "apps/rn/tests/e2e/no-bills-branch.spec.ts",
    "token": "the tier does not change what is owed"
  },
  "S1P1-B5-MF6": {
    "what": "pass-1 B5 — the property MF.6 was built for, which the fix must not undo: with the premium Recovery Plan on screen this card states the obligations WITHOUT a competing plan of action. Plant-verified: deleting `shortfallAdviceOwnedElsewhere` reds this AND two pre-existing recovery.spec guards",
    "file": "apps/rn/tests/e2e/no-bills-branch.spec.ts",
    "token": "MF.6 preserved"
  },
  "S1P1-B5-OBLIGATIONS": {
    "what": "pass-1 B5's third consequence — `rows.length + unfunded.length` double-counted a partially-funded bill (a `Pay X (partial)` row AND a `Finish X` remainder), reading 6 for 5 obligations. ⚠️ The fixture needs TWO fully-unfunded bills or the two error modes cancel: strip the engine's `targetId` and every expense remainder collapses to `e:undefined`, which offsets the missing merge exactly — measured, both spellings returned 4 on a one-bill fixture",
    "file": "apps/rn/tests/e2e/no-bills-branch.spec.ts",
    "token": "the count is of OBLIGATIONS"
  },
  "S1P1-M5-CHAIN": {
    "what": "pass-1 M5 — test:gate-plants, the only thing in the tree asserting a gate fails CLOSED, was in NO chain: not run-gates.ts, not validate:release:rn, not CI. A repo-wide search returned 11 files and no runner. All 15 of S0's majors were gates green while doing less than they claimed, and REVERIFY4-4's only behavioural guard is one of its scenarios",
    "file": "scripts/run-gates.ts",
    "token": "test:gate-plants"
  },
  "S1P1-M6-BOUNDARY": {
    "what": "pass-1 M6 — present()'s word-boundary branch was reached only when the WHOLE token was identifier-shaped, so an identifier-PREFIXED token (`function isClamp`) fell back to text.includes and survived the rename it exists to catch. Measured: 3 of 18 tokens failed open on exactly that class. The boundary is now decided per END, with `-` in the name charset so a kebab name cannot grow past it either ⛔ S1.9.4 [pass-2 B-1] — the token now names THE LINE THAT WOULD HAVE TO CHANGE, not an identifier the un-fix leaves in place; the behavioural proof is the test:gate-plants scenario / self-check.",
    "file": "scripts/check-finding-guards.ts",
    "token": "const lead = startsWithName"
  },
  "S1P1-M7-CODELINE": {
    "what": "pass-1 M7 — 5 of 18 tokens survived on a DOCSTRING line alone, so deleting the assertion left the gate green; GUARDED-5 composed with GAP-2 into a fully silent removal of an invariant. A token must now appear on a non-comment line. Mechanism A fixed separately: S1-M9-GUARDIAN's token sat on the PRECONDITION line, so the finding's own assertion could be deleted with the gate green ⛔ S1.9.4 [pass-2 B-1] — the token now names THE LINE THAT WOULD HAVE TO CHANGE, not an identifier the un-fix leaves in place; the behavioural proof is the test:gate-plants scenario / self-check.",
    "file": "scripts/check-finding-guards.ts",
    "token": "if (!presentInCode(text, e.token))"
  },
  "S1P1-M8-STRICT": {
    "what": "pass-1 M8 — MIN_ENTRIES was 24 against 34 entries and checked with `<`, so ten closed findings could be deleted with the gate green; MAX_UNGUARDED used `>` and would acquire the same slack on the next guarded entry. Both are strict equality now, matching MAX_EXEMPT in the same commit range. Plus a duplicate-id check, because JSON.parse silently keeps the last of a repeated key ⛔ S1.9.4 [pass-2 B-1] — the token now names THE LINE THAT WOULD HAVE TO CHANGE, not an identifier the un-fix leaves in place; the behavioural proof is the test:gate-plants scenario / self-check.",
    "file": "scripts/check-finding-guards.ts",
    "token": "ids.length !== MIN_ENTRIES"
  },
  "S1P1-M9-ROOTS": {
    "what": "pass-1 M9-A — the S1 surface roots were four directories plus ten hand-named files, i.e. an ENUMERATION, inside the file whose own docstring says an inclusion list fails silent. store/ was 6 of 88, data/ 3 of 21, (tabs)/ 1 of 4, and (tabs)/index.tsx — 1,087 lines, where blocker B5 is wired — was on no surface at all. Widened to whole directories with routed exclusions: 72 files became 137",
    "file": "scripts/surface-coverage.s1.json",
    "token": "apps/rn/src/app/(tabs)/index.tsx"
  },
  "S1P1-M9-VOCAB": {
    "what": "pass-1 M9-B — unswept was `every(v => v === 'never' || 'unknown' || 'partial')`, so ANY other string counted as SWEPT: `nevr`, `Never`, ` never` all silently converted an unread file into a read one, in the count and in the generated inventory. The vocabulary is now an allow-list checked before anything is counted ⛔ S1.9.4 [pass-2 B-1] — the token now names THE LINE THAT WOULD HAVE TO CHANGE, not an identifier the un-fix leaves in place; the behavioural proof is the test:gate-plants scenario / self-check.",
    "file": "scripts/surface-coverage.ts",
    "token": "if (!VALID_CLAIMS.has(v))"
  },
  "S1P1-M9-ROUTING": {
    "what": "pass-1 M9-C — an exclusion could route a file to a surface that does not exist, and 13 did: they named `s4`, which has no SURFACES entry and no claims file. The docblock's rule (every exclusion names the surface that owns the file) was enforced by nothing. Routings are now typed and validated against KNOWN_SURFACES, which deliberately includes not-yet-built surfaces and an explicit `none` ⛔ S1.9.4 [pass-2 B-1] — the token now names THE LINE THAT WOULD HAVE TO CHANGE, not an identifier the un-fix leaves in place; the behavioural proof is the test:gate-plants scenario / self-check.",
    "file": "scripts/surface-coverage.ts",
    "token": "if (r && !KNOWN_SURFACES.has(r.to))"
  },
  "S1P1-M10-AUTHORING": {
    "what": "pass-1 M10 — an audit report quoting a credential shape as evidence reds lint:secrets, and therefore lint:rn, on every committed tree; it happened on 2 of 2 rounds, the second while quoting the gate's own evidence. The author got no warning because untracked files are invisible to the committed scan BY DESIGN, and both exits were refused by the instrument's own text. `--working-tree` is an authoring-time read that loosens nothing and is deliberately NOT in lint:rn ⛔ S1.9.4 [pass-2 B-1] — the token now names THE LINE THAT WOULD HAVE TO CHANGE, not an identifier the un-fix leaves in place; the behavioural proof is the test:gate-plants scenario / self-check.",
    "file": "scripts/check-committed-secrets.ts",
    "token": "if (WORKING_TREE) {"
  },
  "S1P1-B2-INVALIDATE": {
    "what": "pass-1 B2 — intentRollback snapshots the ENTIRE DebtStore and undoIntentAction restores the whole thing; nothing ever cleared it, so a debt, a goal, a bill and a strategy change made after logging a payment were all destroyed (and persisted) by an Undo whose card promises only to undo the payment. Fixed as a CLASS in the set wrapper: any patch that moves `store` without mentioning `intentRollback` is somebody else's write. ⚠️ The wrapper was also made unconditional — it used to fall through to the raw setter when neither `bound` nor `refuse` was passed, which is every store a test builds",
    "file": "apps/rn/src/store/storeActions.test.ts",
    "token": "an unrelated edit INVALIDATES the whole-store snapshot"
  },
  "S1P1-B2-DOORS": {
    "what": "pass-1 B2's second and third doors, which the auditor flagged as read-only inference and are now measured: importStore (restore a backup) and reset() (delete all data) both left the snapshot intact, so a payment card could replace a freshly-restored portfolio with the pre-restore one. A per-action fix would have been a LIST and would have missed exactly these",
    "file": "apps/rn/src/store/storeActions.test.ts",
    "token": "importStore (restore a backup) invalidates it too"
  },
  "S1P1-B2-CARD": {
    "what": "pass-1 B2 — the Today Undo card had NO end-to-end coverage at all; the only test called logManualPayment and undoIntentAction with nothing in between. This is the property that must SURVIVE the fix: an over-eager invalidation would delete the card from the product and nothing else would notice. ⚠️ Navigation must be by TAB, never page.goto — intentRollback is transient by design, so a reload would find it missing and read as a break",
    "file": "apps/rn/tests/e2e/intent-undo.spec.ts",
    "token": "logging a payment still offers the Undo on Today"
  },
  "S1P2-D2-3-POSITIVE": {
    "what": "pass-2 D2-3 - the only test of the no-paycheck Today was one bare toHaveCount(0) under a comment claiming 'the app still boots to Today' and 'no empty shell', both of which are exactly what an absence assertion is true of when nothing rendered. Measured: one hit in the whole test tree for this state, and it is another absence assertion",
    "file": "apps/rn/tests/e2e/guardian.spec.ts",
    "token": "Today prompts for a paycheck"
  },
  "S1P2-CM1-FIELDLABEL": {
    "what": "pass-2 C-m1 - the repairs card printed raw SCHEMA KEYS ('Chase - minimumPayment') for five fields of six, while the mechanism for a human string already existed and had been applied to the sixth. The map fixes the instance; the test gates the CLASS against migrations.ts's own REPAIRABLE_MONEY_FIELDS, both directions",
    "file": "apps/rn/src/components/plan/dataRepairsCopy.test.ts",
    "token": "can be repaired and has no user-facing name"
  },
  "S1P2-B2-STRICT": {
    "what": "pass-2 B-2 - check-audit-closure's two downward-only caps used `>`, which is the [M8] slack shape its sibling had fixed one file over in the same range. Zero slack today, so no instrument was blinded - it becomes a major the first time either count improves, because 55 -> 54 opens a permanent slot for a closure to stop tracing unseen",
    "file": "scripts/check-audit-closure.ts",
    "token": "if (d37Untokenised.length !== MAX_UNTOKENISED.d37)"
  },
  "S1P2-B3-FIGURE": {
    "what": "pass-2 B-3 - plan-hero-conserves' docstring claimed 'the specific measured figures are asserted too, so a hero that renders no segments cannot satisfy 0 === 0'. True of the healthy test and FALSE of the shortfall one, which is the test carrying S1P1-M4-CONSERVES and asserted no figure at all",
    "file": "apps/rn/tests/e2e/plan-hero-conserves.spec.ts",
    "token": "Required names what the paycheck COVERED"
  },
  "S1P2-D2-1-AGREE": {
    "what": "pass-2 D2-1 - computeState's docblock requires the card, selectPlanSummary and the forecast to derive the band from one function 'so they can never disagree', and all three called it with DIFFERENT first arguments, differing by exactly appliedTopUp. On the designed path the card turned Clear after its own one-tap offer while its own 'See forecast' button opened cycle 0 on 'Tight - $50 under'. Asserted as an AGREEMENT, not three values",
    "file": "apps/rn/src/store/guardianSelectors.test.ts",
    "token": "the three producers agree AFTER the card"
  },
  "S1P2-D2-1-CYCLE0": {
    "what": "D2-1's over-fix, found because a plant of it stayed GREEN - applying the surplus to every projected cycle passed every other assertion, since they all agreed about cycle 0 and nothing looked further out. The money is in checking THIS cycle; the waterfall refills the goal next cycle, so a projected cycle counting it forecasts a cushion nobody will have",
    "file": "apps/rn/src/store/guardianSelectors.test.ts",
    "token": "cycle 0 and NO projected cycle"
  },
  "S1P2-D2-1-ROOTS": {
    "what": "pass-2's unclassifiable finding - packages/core/timeline was on NO surface, so the forecast module the pass's sharpest major (D2-1) is ABOUT was invisible to the pass reporting on it. The plan called it one line; measured, 18 of 21 packages/core directories were off-surface (102 files, `debt` alone 53), and apps/rn/src had 184 more under no root at all",
    "file": "scripts/surface-coverage.ts",
    "token": "'apps/rn/src',"
  },
  "S1P2-SURFACE-COMPLETE": {
    "what": "the CLASS behind five root corrections - M9, [D73], packages/core and apps/rn/src each closed one instance and none closed the class. This gate sources its file list from `git ls-files` (a walk with a deny-list showed both failure modes on its first two runs: matching top-level paths only let apps/rn/node_modules through, then apps/rn/core, a symlink to packages/core, reported as 21 invisible directories) and requires every tracked source file to be under some surface's roots, cap 0 downward-only",
    "file": "scripts/surface-coverage.ts",
    "token": "const MAX_INVISIBLE = 0;"
  },
  "S1P2-SKIP-EXPIRY": {
    "what": "the completeness gate's skip list excludes the legacy Next surface because P6.11 DELETES it, not because anyone audited it - so the exclusion carries its own expiry: a skip naming a directory that no longer exists reds, and the deletion cannot leave a standing permission behind it. Caught three stale entries on its first run",
    "file": "scripts/surface-coverage.ts",
    "token": "the skip list names director(ies) that no longer exist"
  },
  "S1P2-B1-PLANTS": {
    "what": "pass-2 B-1 - seven registry entries were pinned by an identifier the un-fix leaves in place, and ALL SEVEN stayed green with the defect restored; three of them the fixes to check-finding-guards.ts itself, the gate that certifies the rest. test:gate-plants covered none of the seven gates. These six scenarios plant a real defect of each gate's own class",
    "file": "scripts/test-gate-plants.ts",
    "token": "...B1_SCENARIOS,"
  },
  "S1P2-B1-REASON": {
    "what": "pass-2 B-1's harness half - an exit-code-only check scores a pass when a gate reds for a DIFFERENT reason than the planted defect; measured on the M9-vocab un-fix, where the gate still exited 1 for the inventory reason and the scenario would have counted as sound",
    "file": "scripts/test-gate-plants.ts",
    "token": "const rightReason = !s.expect || withPlant.out.includes(s.expect)"
  },
  "S1P2-B1-SELFCHECK": {
    "what": "pass-2 B-1's un-plantable seventh - the routing check reads a predicate in surface-coverage.ts rather than a file, so no scenario can hand it a defect; the first self-check asserted the PREDICATE and stayed green with `if (false && ...)` planted at the call site, which is tested-helper-is-not-a-used-helper written into the fix for it",
    "file": "scripts/surface-coverage.ts",
    "token": "the routing self-check"
  },
  "S1P2-A1-NETTED": {
    "what": "pass-2 A1 - three reads of the same money and the last fix range moved two of them; a user $1 short after moving $200 at the Guardian's own suggestion was told a $20 purchase would leave them $20 short, in the card saying the $200 holds their line, with $199 unspent. Every prior test fixed topUp 200 against shortfall 400 - the member where a blanket 0 and netting agree exactly",
    "file": "apps/rn/src/store/guardianSelectors.test.ts",
    "token": "the band are ONE expression"
  },
  "S1P2-C2-DEBTS-HERO": {
    "what": "found by C2's after-scan, on the other list - a balance repaired to 0 is not in `active`, so the debts hero's total AND its count both excluded a debt the user still owes; the guard beside it (allCleared) only ever covered the case where EVERY balance is unread",
    "file": "apps/rn/tests/e2e/data-recovery.spec.ts",
    "token": "the hero does not total a portfolio missing an addend"
  },
  "S1P2-C1-RESET": {
    "what": "pass-2 C1 - nothing ever REMOVED a pendingDataRepairs entry, so retyping the balance the card asked for changed nothing and the graduation banner, Money's cleared hero and the Progress trophy were withheld for the life of the install; measured through a reload",
    "file": "apps/rn/src/store/trustSelectors.test.ts",
    "token": "retyping the number ANSWERS it"
  },
  "S1P2-C1-ACK": {
    "what": "pass-2 C1's control, and the one that re-opens a blocker if it moves - A-J2-1 is the rule that the ack hides the CARD and does not un-repair the DATA, so the reset path must distinguish 'the user corrected this field' from 'the user dismissed the card'",
    "file": "apps/rn/tests/e2e/data-recovery.spec.ts",
    "token": "the ACK still answers nothing"
  },
  "S1P2-C2-FIELDS": {
    "what": "pass-2 C2 - both money.tsx goals consumers narrowed the guard to targetAmount while trustSelectors' own docblock said both sides of currentAmount >= targetAmount repair to 0; a goal whose SAVED amount was lost printed its entire target as a remainder with no caption, under a hero saying '33% funded'",
    "file": "apps/rn/tests/e2e/data-recovery.spec.ts",
    "token": "Saved amount could not be read"
  },
  "S1P2-C3-FINALE": {
    "what": "pass-2 C3 - the full-screen finale was gated on celebration?.kind and nothing else, so B1's owner reached three claim sites and not the fourth; measured on one store at one instant, the banner refused 'every balance is cleared' while the finale asserted '$12,400 paid off' three lines away",
    "file": "apps/rn/tests/e2e/data-recovery.spec.ts",
    "token": "the full-screen finale does not fire over a balance nobody read"
  },
  "S1P2-C4-PLAN": {
    "what": "pass-2 C4 - a minimumPayment repaired to 0 produces neither an allocation row nor an unfunded item, so the debt leaves the plan, countOutstandingRequired honestly returns 0 and Today rendered B5's exact sentence in success green over an unpaid $5,000 card",
    "file": "apps/rn/tests/e2e/data-recovery.spec.ts",
    "token": "does NOT read as"
  },
  "S1P2-CLAIM-TABLE": {
    "what": "pass-2 C2/C4's shared cause - the trust rule was wired to a SUBSET of repairable fields three separate ways in one round; migrations.ts now declares what it can repair, trustSelectors routes each one to a claim, and this asserts the two agree in BOTH directions",
    "file": "apps/rn/src/store/trustSelectors.test.ts",
    "token": "NO claim in trustSelectors routes it"
  },
  "S1P2-D2-2-STORE": {
    "what": "pass-2 D2-2, a REGRESSION [B3]'s own fix introduced — one entry per SOURCE accumulates, so undoTightTopUp(source) handed back every cover the source made this cycle; $50 then $30 returned $80 and silently un-covered the earlier purchase. Every prior undo case applied each source ONCE, the member of the class where whole-entry and per-draw agree",
    "file": "apps/rn/src/store/storeActions.test.ts",
    "token": "the Undo returns the LAMP"
  },
  "S1P2-D2-2-SEAM": {
    "what": "pass-2 D2-2's other half — the store fix is invisible unless the CARD passes its draw, and a bare undoTightTopUp('affordability') typechecks perfectly while re-creating the defect; no test asserted that the amount the card DISPLAYS equals the amount its Undo returns",
    "file": "apps/rn/tests/e2e/topup-sources.spec.ts",
    "token": "the card’s Undo returns ITS OWN cover"
  },
  "S1P1-B3-SOURCES": {
    "what": "pass-1 B3 variant A — two independent one-tap money moves shared ONE cycleTopUp record with ONE goalId, so the second overwrote the first's source: $70 out of S1 and $50 out of S2 became a single $120 Undo into S2, leaving S1 permanently short. The aggregate conserved, which is why nothing noticed. Entries are per-source now and each undo finds its own",
    "file": "apps/rn/src/store/storeActions.test.ts",
    "token": "the Guardian card offers its own draw, not the cycle total"
  },
  "S1P1-B3-DOUBLEUNDO": {
    "what": "pass-1 B3 variant B — the affordability card's undo read applied.cover from COMPONENT STATE, never the store, so the Guardian having already reversed the draw was invisible: both undos fired, the goal gained $50 that never existed, cycleTopUp.amount landed at -50 and appliedTopUp()'s Math.max(0,…) hid it. Undo now removes this source's own entry from the store, so a repeat is a no-op by construction",
    "file": "apps/rn/src/store/storeActions.test.ts",
    "token": "the second undo returns its own $50 and no more"
  },
  "S1P1-B3-INVARIANT": {
    "what": "pass-1 B3's related-same-root half — applyTightTopUp recorded the full requested amount while the goal clamped at 0, and recorded it even when goalId matched no goal, so the cushion was credited with money that never moved (100,299 against $200 actually drawn). The invariant nothing asserted: SUM(cycleTopUp entries) === what actually LEFT the goals. `amount` is now derived from the entries and never written independently",
    "file": "apps/rn/src/store/storeActions.test.ts",
    "token": "what actually LEFT the goals"
  },
  "S1P1-B3-LEGACY": {
    "what": "pass-1 B3's back-compat control — a pre-S1.5.3 blob has amount/goalId and no entries. topUpEntries reads it as a single 'guardian' entry so it keeps behaving exactly as it did, or the fix would strand money that is mid-cycle across an upgrade. No migration: the record is cycle-keyed, so a legacy one stops being read at the next rollover",
    "file": "apps/rn/src/store/storeActions.test.ts",
    "token": "an entry-less record still reads as the Guardian"
  },
  "S1P1-B4-FLAG": {
    "what": "pass-1 B4 — money.tsx's `converting` was a useState set by the conversion effect and cleared by NOTHING (grep returned one line, the setter), so it was handed to every later DebtSheet in the Debts section and routed ordinary new debts into convertExpenseToDebt, silently deleting an unrelated bill. Fixed by SCOPE: it lives inside the `sheet` state and dies with the sheet",
    "file": "apps/rn/tests/e2e/misfiled-expense.spec.ts",
    "token": "backing out of a conversion does not arm the next debt to delete that bill"
  },
  "S1P1-B4-ONEWRITE": {
    "what": "S1.5.3 — DebtSheet.submit()'s BNPL branch never consulted convertingExpenseId, so converting a bill into a BNPL ADDED the debt and LEFT the expense: $1,600 reserved as a bill AND projected as a debt at once, which is verbatim the window the non-BNPL branch's comment says the one-write design prevents. ⚠️ Found because a plant FAILED to red a test it should have — both branches now route through one commit()",
    "file": "apps/rn/tests/e2e/misfiled-expense.spec.ts",
    "token": "converting a bill INTO a BNPL is still ONE write"
  },
  "S1P1-B4-PREPARE": {
    "what": "pass-1 B4's second consequence — convertExpenseToDebt was a reduced copy of addDebt that skipped normalizeBnplInstallment, justified by 'a conversion never arrives in that shape'. False: the type picker is on screen during a conversion. The same 4x$50 input stored as $200 via addDebt and $0 via convertExpenseToDebt, and $0 files the debt under PAID OFF. Both share prepareNewDebt; the guard asserts EQUIVALENCE, with a control instance seeded WITH a bill so 'did not delete' is distinguishable from 'nothing to delete'",
    "file": "apps/rn/src/store/storeActions.test.ts",
    "token": "convertExpenseToDebt derives it IDENTICALLY"
  },
  "S1P1-B4-PREFILL": {
    "what": "pass-1 B4's third consequence — DebtSheet seeded `recurrence` from `editing?.recurrence ?? 'monthly'` while name/minimumPayment/dueDate all honoured the prefill, so converting a QUARTERLY or ANNUAL bill filed its amount as a MONTHLY minimum inside the debt-free date",
    "file": "apps/rn/tests/e2e/misfiled-expense.spec.ts",
    "token": "converting a QUARTERLY bill keeps its cadence"
  },
  "S1P1-M1-CALL": {
    "what": "pass-1 M1 - the grouped Expenses list and the 'where it goes' receipt ENUMERATED categories instead of partitioning, so a bill the app could not categorise vanished from both. ⚠️ S1.9.4 after-scan: this token was the bare identifier `resolveBillCategory` and is now the CALL expression - but `money.tsx` holds TWO identical call sites, and a token cannot count occurrences, so a SINGLE-site revert leaves it green. Measured, and the behavioural guards do catch it: reverting the second site alone reds 3 of the 4 tests in bill-category-partition.spec.ts. This entry guards a both-sites removal; S1P1-M1-LIST / -RECEIPT / -SHEET are what guard the behaviour.",
    "file": "apps/rn/src/app/(tabs)/money.tsx",
    "token": "resolveBillCategory(e) === category"
  },
  "S1P1-M1-LIST": {
    "what": "pass-1 M1's user-facing half — the uncategorised bill renders under Other AND a known bill keeps its own heading. Both classes are asserted: the original `e.category ===` plant reds the first, an 'everything resolves to Other' plant reds the second, and either assertion alone leaves the other direction unfalsifiable. The helper waits on a ROW so the heading assertions are reached under both plants",
    "file": "apps/rn/tests/e2e/bill-category-partition.spec.ts",
    "token": "a bill the app cannot categorise still renders"
  },
  "S1P1-M1-RECEIPT": {
    "what": "pass-1 M1's second site — `categoryBreakdown` fed the breakdown sheet through the same enumeration, so 'where it goes' was short by the same bill. Scoped to the dialog: unscoped, every row assertion is also satisfied by the list behind the sheet, so the receipt could render empty and pass",
    "file": "apps/rn/tests/e2e/bill-category-partition.spec.ts",
    "token": "receipt accounts for it"
  },
  "S1P1-M1-SHEET": {
    "what": "pass-1 M1's third site, found by M1's own after-scan — `ExpenseSheet` seeded its picker from `editing?.category ?? 'other'`, which catches an ABSENT category and not an UNRECOGNISED one, so `Select` fell to its `?? 'Select'` fallback and saving an untouched form round-tripped the unreadable value back into the store. Reachable ONLY once M1 gave the bill a row to tap",
    "file": "apps/rn/tests/e2e/bill-category-partition.spec.ts",
    "token": "does not write the unreadable value back"
  },
  "S1P1-M2-SAVED": {
    "what": "pass-1 M2 — the funded branch of a goal row printed `targetAmount` under the label `saved`, so an over-funded goal reported the SMALLER number: '$1,000 saved' over a pot holding $5,000, one inch under a hero that totals `currentAmount` correctly. The two are equal only exactly at the target, and nothing prevents over-funding (GoalSheet never compares the fields; a negative applyTightTopUp can do it alone)",
    "file": "apps/rn/tests/e2e/goal-row-saved.spec.ts",
    "token": "reports what is actually saved"
  },
  "S1P1-M2-BRANCHES": {
    "what": "pass-1 M2's other class — exactly-at-target and short-of-target rows must not move. The at-target row is where the old code was accidentally right, so it proves the fix is a correction and not a substitution; the short row still reads a REMAINDER labelled `left`, and is the only assertion a widened `funded` reds (measured: 'Vacation, Funded, Savings, $500 saved')",
    "file": "apps/rn/tests/e2e/goal-row-saved.spec.ts",
    "token": "the two branches the fix must NOT move"
  },
  "S1P1-M2-UNREADTARGET": {
    "what": "found by M2's after-scan and MEASURED before it was believed — .11.4 stopped a goal with an unreadable target from wearing a Funded pill and then let the row fall to the `left` branch, where Math.max(0, 0 - currentAmount) is $0: 'House Fund, Savings, $0 left' over $500 saved. The guard was on the badge and absent from the sentence beside it, the same shape as this pass's B1 and B5. Also the FIRST e2e over .11.4's own badge guard, which had none on this row",
    "file": "apps/rn/tests/e2e/goal-row-saved.spec.ts",
    "token": "not that $0 is left"
  },
  "S1P1-M3-BAND": {
    "what": "pass-1 M3 — buildGuardianBrief derived the band from computeState(discretionary) on the recorded premise that 'a shortfall drives discretionary to 0 -> at-risk, so it needs no separate branch here'. The seam passes selectDiscretionary + appliedTopUp and the top-up term is not 0, so a shortfall with a top-up on record read CLEAR. The branch the comment called unnecessary is now written",
    "file": "packages/core/guardian/testBuildGuardianBrief.ts",
    "token": "a shortfall is at-risk even with headroom on the input"
  },
  "S1P1-M3-SELECTOR": {
    "what": "pass-1 M3 at the seam that produces it, plus the counter-fixtures. This file had ZERO cycleTopUp cases before (grep -c returned 0), so the one input that falsifies the band's premise was untestable here. Carries the covered-and-tight rows too: `state = at-risk` unconditionally passes the shortfall assertions and only those reds it",
    "file": "apps/rn/src/store/guardianSelectors.test.ts",
    "token": "a shortfall is at-risk even with a top-up on record"
  },
  "S1P1-M3-RENDER": {
    "what": "pass-1 M3's user-facing half, and the ONLY place it can be pinned — the copy branch sets `detail` from shortfall > 0 regardless of the band, so the selector cannot see this. PaydayGuardianCard's gate (stale || pausedDeploy || state === 'at-risk') had no test of any kind. Asserts the Guardian's own sentence: measured, the defective card still contained '$400' via RecoveryPlanSection, so a bare /\\$400/ assertion passed with the defect present",
    "file": "apps/rn/tests/e2e/guardian-shortfall-topup.spec.ts",
    "token": "states its amount on the card even with a top-up on record"
  },
  "S1P1-M3-AFFORD": {
    "what": "found by M3's after-scan and MEASURED — the same mechanism one door over. selectSpendable is 0 on any shortfall, so `+ appliedTopUp` was the whole figure and selectAffordability told a user who could not cover their bills that a $150 purchase was 'tight' (cushionAfter $50) instead of 'short' (shortBy $150). Fixed as a blanket 0 rather than netting, so the two cards cannot sit on one screen disagreeing about the same cushion — the class that seam's own comment records",
    "file": "apps/rn/src/store/guardianSelectors.test.ts",
    "token": "a purchase during a shortfall is SHORT"
  },
  "S1P1-M4-CONSERVES": {
    "what": "pass-1 M4 — PlanHero's `required` was `summary.requiredTotal`, what is OWED, while the headline is exactly `paycheckAmount`; in a shortfall the parts exceeded the whole and `flexGrow` normalised the bar to the full track, so 'Required $1,400 · Spoken for $300' sat under a $1,000 headline looking ordinary. Asserts the INVARIANT (segments sum to the headline) rather than a row of figures, with a parse guard because nulls make 0===0 read as conserving",
    "file": "apps/rn/tests/e2e/plan-hero-conserves.spec.ts",
    "token": "the paycheck split conserves in a SHORTFALL"
  },
  "S1P1-M4-NOTZERO": {
    "what": "pass-1 M4's other class — a healthy cycle must be untouched AND the Required segment must still carry its real value. `required = 0` conserves trivially whenever the whole paycheck lands in Flexible, so the sum assertion alone cannot falsify a fix that simply zeroed the segment; measured under that plant, both tests red on the parse guard",
    "file": "apps/rn/tests/e2e/plan-hero-conserves.spec.ts",
    "token": "the branch the fix must not move"
  },
  "S1P1-B5-PROXY": {
    "what": "pass-1 B5's fallout — recovery.spec's P1-4 test asserted page-wide that \"Essential 11\" appeared nowhere, a PROXY for the cover-now line that held only while nothing else on Today named the bills. Restoring the obligations reddened it without `summariseNames` changing. Rescoped to `recovery-cover-now-names` and plant-verified against P1-4's original run-on",
    "file": "apps/rn/tests/e2e/recovery.spec.ts",
    "token": "recovery-cover-now-names"
  },
  "S1P3-A1-BUDGET": {
    "what": "pass-3 A1 — cannotAmortize compared remaining interest against the SHRINKING active-minimum sum, not the constant monthlyBudget the loop spends, so it bailed out of a plan that amortizes the instant the first debt cleared. The guard asserts the UNPAYABLE sentinel is absent AND pins the debt-free DATE to the payoff CHART's zero-crossing — the two producers that disagreed. Verified to red on the restored defect; the `monthlyBudget > 0` half is separately covered by the one-time-BNPL test",
    "file": "packages/core/debt/testDebtProjection.ts",
    "token": "the debt-free DATE and the payoff CHART agree on the same plan (S1P3-A1)"
  },
  "S1P3-A2-INWINDOW": {
    "what": "pass-3 A2 — buildCycleSnapshot summed the raw per-installment minimumPayment while the rollover deducted the window-scaled one, so History reported $100 for a cycle that paid $200. The guard pins the snapshot to what applyRolloverPayment ACTUALLY deducts rather than to a literal, so the two cannot drift together, and carries a non-BNPL control. Every prior fixture in that file was monthly — the one member of the class where scaled and unscaled agree",
    "file": "packages/core/testing/testPayCycleHistoryRegression.ts",
    "token": "History reports the money the rollover actually deducted (S1P3-A2)"
  },
  "S1P3-A4-CADENCE": {
    "what": "pass-3 A4 — the in-window seams were gated on isInstallmentNative, so a fallback BNPL (type bnpl + recurrence + dueDate, no installment fields; reachable via CSV import and pre-2.7.2 backups) was reserved and paid down at 1x while the chart and date rated it by cadence. Jason chose the reserve moves to the cadence. The token names the PAYDOWN assertion, not the count — proven independently load-bearing by relaxing the two assertions before it (D3-3's lesson: name the line that USES the check)",
    "file": "packages/core/debt/testBnplInstallment.ts",
    "token": "PAID DOWN by the same amount, so reserve and paydown stay in lockstep (S1P3-A4)"
  },
  "S1P3-C1-ROWFIGURES": {
    "what": "pass-3 C-1 — the claim table's 'row-figures' route had ZERO production consumers (3 grep hits, all the declaration or its own test), so Money printed \"0% APR\" on a card charging 22% and \"$0/mo\" on one demanding $150. The token names the ABSENCE assertion rather than the honest-state one: verified red under the plant twice, once normally and once with the honest-state assertions relaxed, so both halves are independently load-bearing",
    "file": "apps/rn/tests/e2e/trust-claims.spec.ts",
    "token": "await expect(row).not.toHaveAccessibleName(/0% APR/);"
  },
  "S1P3-C1-GATE": {
    "what": "pass-3 C-1's structural half — trustSelectors.test.ts gates the table's INPUTS (every repairable field is routed) and nothing gated its OUTPUTS, which is how a route shipped with no callers. lint:trust-claims reds when any MoneyClaim has no production consumer, when a call site asks a field its claim does not route (the intersection silently reads \"readable\"), and when the unguarded-claim-site ledger goes stale. All three plant-verified",
    "file": "scripts/check-trust-claims.ts",
    "token": "is declared in ${TRUST_MODULE} and asked by NO production file"
  },
  "S1P3-C2-SUMS": {
    "what": "pass-3 C-2 — the sums on both expense screens had no guard, so Everyday Spending headlined $120 over a true figure of at least $520 and Money captioned \"of $55 recommended each paycheck\" against $1,400 of rent. ⚠️ The direction is the finding: a dropped addend makes a RECOMMENDATION smaller, so the failure runs toward the user under-reserving. The token names the headline's own testID, which exists because \"$120\" is also the Gas row's amount and a page-wide assertion could not tell them apart",
    "file": "apps/rn/tests/e2e/trust-claims.spec.ts",
    "token": "living-reserve-headline"
  },
  "S1P3-C3-QUANTITY": {
    "what": "pass-3 C-3 — selectHistorySummary computed balance REDUCTION and history.tsx printed it as money \"paid down\", in success green; deleting a duplicated import credited the user with $2,923 they never paid. Fixed by making the quantity match the sentence (sum totalPaidThisCycle), with guardianSelectors' identical expression collapsed onto the same owner. The CONTROL is the load-bearing half — it reds under the plant too, because the pre-fix quantity is a different number",
    "file": "apps/rn/tests/e2e/trust-claims.spec.ts",
    "token": "History still anchors on money that WAS paid"
  },
  "S1P3-C4-ARCHIVE": {
    "what": "pass-3 C-4 — the trophy shelf gated on hasUnreadDebtBalances (balance only) while the claim table routed originalBalance to 'debt-balances', so a cleared $12,000 card was filed \"$0 paid off\" and shared that way. Fixed in selectPaidOffDebts so BOTH mount points inherit it — progress.tsx:346 carries no trust check at all and needs no debt-free state to render. NOT fixed by widening hasUnreadDebtBalances: that would gag the graduation banner over balances read perfectly",
    "file": "apps/rn/tests/e2e/trust-claims.spec.ts",
    "token": "await expect(page.getByText('$0 paid off')).toHaveCount(0);"
  },
  "S1P3-C5-PAYWALL": {
    "what": "pass-3 C-5 — the paywall named a $100 shortfall on a cycle $500 short and sold a Recovery Plan sized against it. mayStatePlanFigures is a REQUIRED parameter rather than an internal check, so the next consumer cannot be added without answering the question. The cushion assertion is the token, not the shortfall one: it covers the NON-shortfall branch, and it was proven independently load-bearing by relaxing the shortfall assertion under the same plant",
    "file": "apps/rn/src/store/paywallLead.test.ts",
    "token": "…and neither is the cushion, which is the branch that runs when the cycle looks fine"
  },
  "S1P3-C6-CALENDAR": {
    "what": "pass-3 C-6 — a BNPL plan whose scheduledPaymentAmount could not be read left the installment expansion and was listed as ONE payment of four, with no \"+ N more\" and a month subtotal reading as the month's whole BNPL load. The unread plan's degraded row is now dropped and the plan named. The token is the absence assertion on the short list, verified red with the honest-state assertion relaxed",
    "file": "apps/rn/tests/e2e/trust-claims.spec.ts",
    "token": "await expect(page.getByText(/1 payment/)).toHaveCount(0);"
  },
  "S1P3-D3-1-WIDGET": {
    "what": "pass-3 D3-1 — the Home/Lock Screen said \"Debt-free · 100% · $0\" over balances mayClaim('debt-balances') was refusing at the same instant: B1's finding verbatim with the widget standing where Today used to. No fixture in the repo had ever put a pendingDataRepairs entry through buildWidgetSnapshot. The token names the PERCENT assertion, not the date one, because the finding's own warning is that repairing only the date leaves \"100% · $0\" — the same falsehood without the word; plant-verified as its own scenario",
    "file": "apps/rn/src/widget/widgetSync.test.ts",
    "token": "…and the ring does not say 100%, which is the same falsehood without the word"
  },
  "S1P3-D3-2-SPOKEN": {
    "what": "pass-3 D3-2 — Siri and the Lock-Screen Live Activity said \"$1,080 free to put toward debt\" when the obligation netted out was one the app could not read; measured against a control that moves the figure $180 -> $1,080 on that single variable. Both nothing-to-say returns already existed and their callers already handled them, so the whole path was there and the CALL was missing. The Live Activity token names the LIFECYCLE assertion — a null payload must END a running activity, not freeze it on the last false figure — and its control pins that the end comes from the trust guard rather than from the window or the toggle",
    "file": "apps/rn/src/liveActivity/paydayActivityContent.test.ts",
    "token": "…and a running activity is ENDED, not left showing the last figure"
  },
  "S1P3-D3-CAPS": {
    "what": "S1.10.6.3's own after-scan — check-trust-claims.ts shipped with MAX_EXEMPT/MAX_OPEN computed as Object.keys(X).length, so both downward-only caps were VACUOUS: a cap derived from the list it caps can never be exceeded. Found by re-reading the gate while removing D3-1's row, not by any suite. Now literals, and plant-verified by adding a row above the cap",
    "file": "scripts/check-trust-claims.ts",
    "token": "This cap only goes DOWN."
  },
  "S1P3-B3-UNKNOWN": {
    "what": "pass-3 blocker B3 — backupToCloudGuarded branched on `unclaimed` alone, so an `unknown` remote (iCloud UP and stat() throwing) went through to backupToCloud, which re-checked isAvailable(), got true, and WROTE OVER another device's backup; the install then recorded its own clock as the file's identity and refused every later backup forever. The guard's own docblock claimed `unknown` was already refused downstream — true of one of the two ways it is produced, false of the other. This file's only coverage stopped at inspectRemote, and neither writeCount assertion was on an `unknown` claim. The token names the WRITE-COUNT assertion, proven independently load-bearing by relaxing the two before it",
    "file": "apps/rn/src/storage/cloudBackup/service.test.ts",
    "token": "ZERO writes: the other device’s copy is still in the container"
  },
  "S1P3-B3-MTIME": {
    "what": "pass-3 B3's second half — new Date(undefined|NaN).toISOString() THROWS while new Date(null|0) silently becomes 1970-01-01, which inspectRemote then compares as a real file identity; both directions were unhandled. ⛔ The finding's own remedy (`return null`) would have been the clobber: inspectRemote reads null as `none`, which the guard PERMITS. metadataFromMtime throws instead, which becomes `unknown` and is refused — and the rule lives in the pure provider module because the iOS provider imports a TurboModule no runner can load, which is why it had neither owner nor test. Both the throw and the none-vs-unknown direction are plant-verified",
    "file": "apps/rn/src/storage/cloudBackup/service.test.ts",
    "token": "a file with no usable mtime is `unknown`"
  },
  "S1P3-B4-ADAPTER": {
    "what": "pass-3 B4 — createAdapter.web.ts wrapped getItem AND JSON.parse in one catch{return null}, so a truncated write read as 'nothing is stored': persistence.ts ran the v1.6 legacy import over a device that already had a v1.7 store and the first autosave overwrote the last copy. NEITHER adapter had a unit test and no store test parses a blob (MemoryStorageAdapter has no parse step); the one corrupt-bytes e2e seeds JSON.stringify('this is not a store'), which is VALID JSON and the single member of the class both adapters agreed on. This is the first unit test either adapter has had",
    "file": "apps/rn/src/storage/createAdapter.test.ts",
    "token": "corrupt bytes come back as the RAW STRING"
  },
  "S1P3-B4-E2E": {
    "what": "pass-3 B4's user-facing half — the truncated-bytes member added BESIDE the existing valid-JSON fixture, never in place of it, so one uncovered member is not traded for another. Asserts the same outcome as the valid-JSON case by design: the whole finding is that two members of one class behaved differently",
    "file": "apps/rn/tests/e2e/data-recovery.spec.ts",
    "token": "seedTruncated"
  },
  "S1P3-C7-LOSSES": {
    "what": "pass-3 C-7 — describeBackup was byte-identical for an intact backup and one the reader had just recorded three losses on, and the user learned about them only after the irreversible replace. The answer was already inside the object being described (result.store.pendingDataRepairs). All seven existing describeBackup assertions used healthy fixtures. The token names the COUNT assertion rather than the naming one, proven load-bearing by relaxing the naming assertion under the same plant",
    "file": "apps/rn/src/data/readBackup.test.ts",
    "token": "…and counts it, so \"one\" is distinguishable from \"nine\""
  },
  "S1P3-C7B-CLOUDDOOR": {
    "what": "pass-3 C-7b — the iCloud restore confirm described NOTHING: no counts, no losses, because it confirms before it fetches. The remedy is a PRE-READ (previewRestore), not a wording change, which the finding says explicitly because a sentence-only fix could not work. Both doors compose from one owner (describeRestorePreview) so they cannot drift. ⛔ Paired structurally in check-copy-owners as well, because this screen's `ready` branch is unreachable to every automated test in the repo — its own docblock records a defect surviving thirteen lenses for that reason",
    "file": "apps/rn/src/data/readBackup.test.ts",
    "token": "C-7b — and it names the loss, exactly as the file door does"
  },
  "S1P3-B2-APRBOUND": {
    "what": "pass-3 B2 — the two RN hand-entry paths were the ONLY APR paths with no 0-100 bound, so `2599` (a missing decimal in 25.99 on a decimal-pad field labelled 'APR %') was saved and planned against as 2599%: $10,829.17/mo of interest on a $5,000 card, ranked first under avalanche, with a debt-free date computed from it. ⚡ A unit test asserted the bound and PASSED, because parseDebtFormValues' only live consumer is the legacy root tree — the guard travelled with v1.6 and never crossed to RN, while RN kept the error STRING for the same field and narrowed what triggers it to 'unparseable'. Asserts what LANDED in the store, not that an error appeared; the control pins 25.99 and the exact-100 boundary. Plant-verified: removing the bound reds the refusal test and leaves both controls green",
    "file": "apps/rn/tests/e2e/amount-guards.spec.ts",
    "token": "Enter an APR between 0 and 100."
  },
  "S1P3-B7-BREADCRUMB": {
    "what": "pass-3 B7 — the Sentry breadcrumb scrub redacted the amounts and passed the CREDITOR NAMES: 'Visa, Focus, $[redacted] · 22.99% APR, estimated verified Jun 3' left the device on a build with a live DSN. ⛔ The remedy is not a wider regex — a name cannot be pattern-matched — so touch categories are dropped whole, the way console already is. ⚡ Rated major because of the TEST: its fixture was a user-authored bill name asserted as correctly untouched, pinning the leak green. Four plants: the drop removed, only one of the two SDK spellings listed, the over-fix that drops navigation too (which turns the diagnostic feature off), and the money rule itself",
    "file": "apps/rn/src/utils/scrubBreadcrumb.test.ts",
    "token": "a touch breadcrumb carrying a creditor name is DROPPED"
  }
}
