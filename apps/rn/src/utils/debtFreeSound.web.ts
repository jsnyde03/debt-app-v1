/** VIS-6 web no-op — the debt-free chime is a native delight; this keeps expo-audio + the wav asset out
 *  of the web bundle. Same export surface as `debtFreeSound.ts` ([[feedback_platform_split_reexport_gap]]). */
export function playDebtFreeSound(): void {}
