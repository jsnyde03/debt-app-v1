# CanvasKit wasm is copied from node_modules on install (scripts/copy-canvaskit.mjs), not committed.
/public/canvaskit.wasm

# 3.5.8.7 — the demo beat reference set (`npm run shots:demo`). Regenerated on demand and compared
# against the CI capture artifact; committing it would put ~20 phone-sized PNGs in the tree per re-shoot.
/capture-ref/
# 3.5.8 — the CI capture pipeline writes here on the runner.
/capture-out/

# 4.1.3a — the native-e2e .app cache provenance stamp, written in CI only.
.appcache-provenance.txt
