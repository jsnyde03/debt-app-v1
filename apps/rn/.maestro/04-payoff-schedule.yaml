# Maestro E2E — reaching the PAYOFF SCHEDULE on real UIKit. This is the flow that would have caught the
# bug the web gate missed three times.
#
# History: "View Payoff Schedule" lived in the debt edit sheet's header and opened a sheet from inside a
# sheet. On iOS that meant either a nested Modal (never presented) or an absolute overlay rendered as a
# SIBLING of the presented Modal — behind it, invisible. Web is one DOM tree, so Playwright saw it fine.
# Flow 02 didn't catch it either: it only taps the ✕ on the ADD sheet, where the schedule entry never
# renders (it's edit-only).
#
# 3.7.A0 replaced the nesting with a real route. This asserts the whole journey on the Simulator, which
# has genuine UIKit presentation semantics: EDIT an existing debt → tap the schedule entry → the schedule
# screen is actually VISIBLE, and the edit sheet is gone rather than covering it.
#
# Depends on 01 having run first (the ORDER IS EXPLICIT in native-e2e.yml — Maestro does NOT sort a directory argument; measured 2026-08-12).
appId: com.jasonsnyder.debtplanner
---
- launchApp:
    clearState: false

- tapOn:
    id: "tab-money"

# Viewing a payoff schedule is a READ. These three assertions, and the three at the end, pin that: the
# portfolio must be identical before and after the journey.
#
# They began as instrumentation. A run of this flow ended with the VISA DEBT GONE — 3 debts / $11,580 down
# to 2 / $9,180 — and the answer turned out to be that the tap never reached the schedule row at all: it
# landed on the sheet's destructive Remove (see the swipe note below). The diagnosis is finished, but the
# assertions are worth more than the diagnosis was. A read that mutates is exactly the class of defect that
# is invisible until someone counts, and nothing else in the suite counts.
- assertVisible: ".*Store Card.*"
- assertVisible: ".*Visa.*"
- assertVisible: ".*Car Loan.*"

# Open an EXISTING debt (not "Add debt") — the schedule entry is edit-only, which is exactly the gap
# flow 02 left open.
#
# `.*Visa.*`, not a bare debt name. A `ListRow` spreads `groupLabel`, so the row is one composite a11y
# element whose text is "Visa, $2,400 · 26.99% APR, …", and Maestro matches an element's FULL text as a
# regex — the same thing flow 03 documents and the same reason the tab bar needed ids. This previously
# read `tapOn: "Card"`, which could never match: the demo has "Store Card", "Visa" and "Car Loan", and
# none of them is "Card". Reusing flow 03's selector because it is PROVEN to match a row in this setup.
- tapOn: ".*Visa.*"
- assertVisible: "Edit debt"

# The entry now lives in the sheet BODY and NAVIGATES. If it ever regresses to opening a nested
# presentation, the schedule won't be visible here and this fails on the Simulator — no device needed.
#
# ⚠️ SWIPE the sheet body first, then tap — and `scrollUntilVisible` is NOT enough here.
#
# The row is the last child of the sheet's ScrollView and falls below the fold on a full debt form, but RN
# reports its UNCLIPPED absolute frame: the 2026-08-06 hierarchy dump has it at [20,877][420,925] on a
# 440×956 screen, i.e. centred at y=901 — directly on top of the "Remove" button at ~865–905. So Maestro
# considers it already visible, `scrollUntilVisible` no-ops, and `tapOn: id` taps a destructive control.
# That run only survived because the sheet's Remove now confirms; the run before it deleted the debt.
#
# An explicit swipe actually moves the content. The `assertVisible: "Payoff schedule"` below is what proves
# the tap landed — a visibility assertion here would pass on the clipped element for the same reason.
- swipe:
    start: "50%, 70%"
    end: "50%, 35%"
- tapOn:
    id: "debt-view-schedule"

# The payoff screen actually rendered…
- assertVisible: "Payoff schedule"
# …with real derived content, not an empty shell.
- assertVisible:
    text: ".*debt-free.*"
# …and the sheet that launched it is gone, so nothing is sitting on top of it.
- assertNotVisible: "Edit debt"

# Back returns to Money (a real route, so it has a real back control).
#
# ⚠️ NOT `- back`: that is Android's hardware back button and it did nothing here — the 2026-08-06 run
# reached the schedule, then failed on "Money" with the schedule still on screen. Tap the header control
# instead. It carries `accessibilityLabel="Back"`, but its only text is a "‹" glyph, so it gets a testID
# like every other control this lane drives.
- tapOn:
    id: "screen-back"
- assertVisible: "Money"

# The other half of the read-only invariant. If this fails while the assertions at the top passed, the
# journey itself changed the user's portfolio.
- assertVisible: ".*Store Card.*"
- assertVisible: ".*Visa.*"
- assertVisible: ".*Car Loan.*"
