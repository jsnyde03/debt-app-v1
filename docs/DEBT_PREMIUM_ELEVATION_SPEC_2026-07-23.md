# Debt Premium Elevation — Full Spec **v5** (2026-07-23)

> **v5 changelog** (folds the round-5 focused §2.0 re-check, `DEBT_PREMIUM_ELEVATION_AUDIT_ROUND5_2026-07-23.md` — where buildability + data CONVERGED and mechanics + framing were structural-but-scoped-to-named-fixes). **The one engine fix:** the §2.5 smoothing is rewritten from the v4 two-pass (which traded the false-clear for a false-*alarm*) to a **whole-horizon fixed point + backward water-filling** pre-fund (§2.5). **Build-time pins:** `coldStartHoldback` magnitude; `calibration-maturity ≡ lean-verification`; `cushion_buffer` = realized `min(floor, discretionary)`; `true_leftover` folds to the protected side; `inputsAsOf` = store-level edit stamp (not rollover); `source`-tagged `shouldReAnchor`; windfall excluded from learning; migration/field-type pins; floorless-coldStart intent. **Framing:** a seasonality-aware branch; priority-not-outcome paywall wording; reserve-as-insurance + confident-user attestation; cold-start + observation-metaphor rewording. **Pricing (Jason's call):** de-risk month one by a **stronger confidence-independent month-one proof of value** (§2.10, §2.0.d) — NOT a trial or guarantee. This is the artifact the **full round-6 audit** re-checks for full convergence.
>
> _v4 changelog (retained): the §2.0 rework across framing/mechanics/data (per-signal gates + masking, holdback composition, the canonical bucket list, §3 re-derived)._
> _v3 changelog (retained): the confidence layer, F1/F3/F4 corrections, the F7 reversal, the state-machine unification, positioning + honesty micro-fixes._
> _v4-header (superseded, kept for provenance):_ (scoped to §2.0 — the *only* section round 4 left structural; `DEBT_PREMIUM_ELEVATION_AUDIT_ROUND4_2026-07-23.md`. Every other section reached consensus in round 4 and is unchanged from v3.) Reworks the Confidence-Governance layer across its three facets: **FRAMING** (the "earn-trust" narrative — the gating is the *pitch*, not a hedge; the held reserve is a *visible* bar zone; week-one leads with the un-gated floor auto-protect; the variable-income degrade *plans conservatively*, never abandons; "watch" always qualified private/on-device); **MECHANICS** (drop the undefined `readConfidence` scalar for explicit per-signal gates with per-income **masking** so fixed-income users aren't stuck; one **holdback-composition rule**; the two-pass **false-clear fix** in §2.5; the canonical N-bucket list in §2.2); and **DATA** (§3 re-derived from §2.0's real needs). `[BUILD]` tags mark genuinely test-first details. This is the artifact the **focused §2.0 re-check** audits.
>
> _v3 changelog (retained): folded round-3 findings — the confidence layer, F1/F3/F4 corrections, the F7 reversal, the state-machine unification, positioning + honesty micro-fixes._

IDs: **D…** = review decisions · **C…/F…** = round-2/round-3 findings.

---

## 1. Premium identity, positioning & the ladder

- **Identity = "it watches + acts so you don't have to."** Monitoring + automation of effort; free is a real companion, premium removes the work.
- **Positioning (v3, round-3 competitor):** **retire the two decaying claims** — "un-chattable" and "watches so you don't have to" (frontier assistants now have memory + scheduled tasks; synced rivals literally watch your bank). **Lead with the two that hold:** **(a) engine-grounded honesty** — it can't fabricate your numbers; **(b) automated, payday-cadence, debt-payoff-first planning** — zero YNAB-style hand-budgeting, aimed at the empty lane no incumbent occupies (debt-first × payday-cadence × fully-automated × on-device). Manual entry is the deliberate privacy tradeoff, not a hidden weakness.
- **The price test** (run on every feature 2.3–2.11): *removing it must remove WORK, not just info.*
- **Four-tier ladder:** Free · **Premium ~$4.99** (automation, on-device) · **Premium Connected +$5** (opt-in Plaid, ~v1.8 — **convenience, never accuracy**; retention claims scoped to *our* backend, Plaid named) · **Ava AI** (future).
- **Privacy = "private by default."** The **"E2EE" word is used only if verified true (F-trust #8): [BUILD] confirm iOS exposes an ADP-status API; if not, say "encrypted iCloud backup" everywhere and frame ADP as user-controlled education without claiming detection.** Reconcile the stale "E2EE-by-construction" line in the capstone strategy doc.
- **One free/premium line:** *"free tells you; premium does it."*
- **Distribution:** the differentiated Guardian **is** the marketing lever → a **demo-safe premium showcase (§3.6) is launch-critical**; GTM/ASO is first-class.

---

## 2. The Payday Cushion Guardian

### 2.0 Confidence-Governance layer (reworked in v4 — the §2.0 pass, Theme A)
The Guardian's read is only as trustworthy as its inputs, so it **acts on what it's sure of from day one, and does more as it learns you.** This is the tier's headline promise, not a hedge: *"it protects your floor from the first paycheck, and puts more to work as it learns your income — it won't gamble your rent on a guess."* The confidence layer is what lets the Guardian be **decisive where it's certain and visibly cautious where it isn't**, instead of either over-promising or drowning every sentence in disclaimers.

**The layer's job is to answer two questions each read — *how much may I deploy?* and *how loudly may I say it?* — from the same signals, and to make its caution VISIBLE rather than silent.**

#### 2.0.a — The signals (one canonical uncertainty taxonomy, per-income masking)
Four signals, each with an explicit **applicability** per income type. A signal that is **N/A is MASKED (dropped from the computation), never scored 0** — masking is the fix for the round-4 "fixed-income users stuck permanently low-confidence" bug. There is **no combined `readConfidence` scalar** (round 4: it was undefined and mis-combined); each gate below reads the specific signal(s) it needs, and the voice layer picks the single dominant *live* (non-masked, above-threshold) uncertainty.

| Signal | What it measures | Producer | Applicability |
|---|---|---|---|
| **bill-completeness** | Has the obligation set proven complete? | genuine-cycle counter + surprise-outflow log (§3, NOT `cycleHistory.length`) | **all** income types |
| **read-freshness** | Are the *inputs to this read* (income, bills, balances) recent? | read-level freshness stamp (§3), reconciled with per-debt `lastVerifiedDate` | **all** |
| **lean-verification** | For variable income, how many actuals confirmed the lean *from the low side* (§2.3) | income-actuals log (§3) | **variable only** — MASKED (N/A) for fixed income |
| **income-modelability** | Is income representable by a lean scalar, or high-variance/seasonal? | §2.3 classifier | **all** — resolves to "modelable" for fixed income |

A fixed-income user's confidence therefore rests on bill-completeness + read-freshness + modelability(=high); with a complete bill list and fresh inputs they reach full confidence in a few genuine cycles and the buffers release — they are **not** blocked on the masked lean signal.

#### 2.0.b — Action gating (how much to deploy) + the ONE holdback-composition rule
Four mechanisms can hold cash back on a single paycheck. Round 4 found they were never composed, producing either double-holdback or a swallowed buffer. **The canonical rule (all four in one place):**

```
deploy = max(0, discretionary − floor − combinedHoldback)
combinedHoldback = prefundedReserve + max(discoveryHoldback, coldStartHoldback)
```

- **floor** — always subtracted first; the confidence-INDEPENDENT protection that works on day one (this is the un-gated 2.4 auto-protect — the week-one proof of value).
- **prefundedReserve** — cash earmarked *this* cycle to cover a *specific future* crunch the §2.5 forecast predicts. A real future need, so it **adds** (it does not overlap the uncertainty holdbacks).
- **discoveryHoldback** — while bill-completeness is unproven (first **N=3** genuine cycles, [BUILD]), hold a fraction (default **40%** of above-floor headroom) against un-modeled surprises, decaying to 0 as completeness is demonstrated.
- **coldStartHoldback** — until **≥N=4** actuals confirm the lean from the low side (variable income only; N/A→0 for fixed), hold a **conservative fraction of above-floor headroom, default 25%** ([BUILD] tunable, §7 — round-5 buildability #1: pinned so the `max()` is evaluable for the variable-income-at-cycle-≥3-but-N<4 segment, where `discoveryHoldback` has decayed to 0). Same fraction-of-headroom form as `discoveryHoldback`, released by lean-verification depth (**≡ §2.9 "calibration-maturity"** — the same N≥4 gate, one signal, two names reconciled).
- Because discoveryHoldback and coldStartHoldback are the *same kind* of thing (uncertainty reserves), they **compose by `max`, not sum** — you don't double-charge a week-one variable user for two names of the same caution. (Round-5 caveat, [BUILD]: `max` under-holds against the *joint* surprise-bill-AND-low-income event; the floor still protects independently so the worst case is slightly-more-to-debt, never a breach; if real week-one variable false-clears surface, switch to a partial sum `max + λ·min`.)
- **Degrade — plan cautiously, don't abandon (round-5 framing #1: seasonality gets its OWN branch, not the generic degrade):**
  - **Seasonal income** (a month-of-year signal, not just high variance): the Guardian commits to the *detected pattern*, it does not promise a convergence that never arrives — *"I can see your income is seasonal — I'll hold steady through your lean months and deploy hard when your big months land."* Learning here reveals the constraint (there's genuinely nothing to deploy in the trough); the copy must say that, not "watching for a stable pattern."
  - **High-variance-but-modelable** income: keep planning with a **wider band + higher shrinkage**; no user-blame, no abandonment.
  - **Genuinely floorless** income (no lean estimable at all): the this-paycheck-only fallback — but stated as a deliberate **protection state**, not stuck-low-confidence: `coldStartHoldback` never releasing here is *by design* (there's no lean to confirm), and the floor still protects day one, so it is never double-charged. State this explicitly (round-5 mechanics F5).
  - The full "I can't see far enough" cutoff is reserved for **stale inputs** (read-freshness beyond `ESTIMATE_STALE_DAYS`), never for variability.

#### 2.0.c — The held reserve is VISIBLE — and framed as insurance, with an actionable attestation (round-4 red-team + round-5 framing #3)
`combinedHoldback` (and `prefundedReserve`) are **not** silent. They render as a **named zone on the cushion bar** ("settling-in reserve" / "held for an upcoming tight cycle"), grouped **visually on the protected side** (never adjacent to "put to work" — it must not read as spendable), with their **own partition category** (§2.2 canonical buckets), so `cushion` = *all protected buckets* and "put to work" never counts held cash as deployed.
- **On release, frame it as insurance that paid off, not money withheld for nothing** (round-5 #3: the decaying reserve otherwise reads as "you held my money for six weeks for nothing" — a wolf cried in bar-zones): *"your settling-in reserve is free — you didn't need it this time, and it's now going to work."*
- **A confident user can act on the caution, not just wait it out:** a *"my bills are complete"* attestation **reduces** (does not skip) the `discoveryHoldback` — so the organized fixed-income user isn't stuck with an un-actionable hold they'll learn to dismiss (the round-5 "trained-dismissal → overdraft in a new skin" path). The floor still protects independently.
The user SEEs the Guardian holding money back — that's the trust-earning. A hold the user can't see (or has learned to dismiss) is a hold that doesn't protect them.

#### 2.0.d — Voice gating (how loudly to say it) — the hedge budget
- **At most ONE hedge per read**, chosen by the dominant *live* uncertainty (priority: stale-inputs > lean-unverified > unproven-accuracy > observation-caution). Masked/below-threshold signals never surface. This kills round-3 hedge-stacking.
- **Reconcile 2.1 ↔ 2.3:** auto-maintenance keeps inputs current, so **read-freshness below the "aging" threshold does NOT trigger a hedge** — the auto-maintained-and-recent case stays fully decisive. The freshness signal keys off *read-level* input recency, not per-debt `lastVerifiedDate` (which `applyRollover` intentionally doesn't advance), so a rolled-over-but-current debt is not falsely hedged (§3 defines the reconciliation).
- **Cold-start reframed as protection, not apology — and worded to what's literally true (round-5 framing #5, #6):** day one the Guardian is already *acting* (floor protected). The voice describes accumulation, **not surveillance** — a manual-entry app observes nothing, it waits for you to log — so: *"Your floor is protected from today. As you log each paycheck, I learn your floor — and put more to work as I do."* During the cold-start window the safety claim is qualified to what the first-four-paycheck math earns: *"planning from the low side while I learn your floor,"* not a flat "planning cautiously = safe." Retire "watching for a pattern" (over-claims observation).

#### 2.0.e — Confidence context is stamped (feeds the §2.9 scorecard honestly)
Every prediction stamp (§3) records the read's **`predictedConfidenceContext`** (which holdbacks were active / whether the read was provisional). The §2.9 scorecard **excludes or down-weights low-confidence/provisional predictions** so it grades the Guardian's *committed* reads, not its deliberate caution — otherwise the scorecard re-inherits the C1 "grades conservatism as if it were a confident miss" flaw via the confidence layer.

> **§2.0 data dependencies (must land in §3 before 2.4.D — see §3):** the genuine-cycle counter + surprise-outflow log (bill-completeness), the read-level freshness stamp (read-freshness), the income-actuals log (lean-verification), the per-prediction confidence context (2.0.e), and the substrate must **not** be debt-gated (it keeps feeding after debt-free, §2.7). These change the schema shape, so they are settled here, not in the build.

### 2.1 The promise & the voice
- **Promise (D1.1):** *"will I make it this paycheck?"* + boundary *"based on what you've entered — not your bank"*; strength = **"private by default."**
- **Voice (D2.1):** active **plan-shaping** ("I've set this paycheck to…"), never custody; completed-action words only for real executions. Subject to the **hedge budget (§2.0)**.
- **False-reassurance guard (D2.2):** clear state is an **observation** ("Looks clear this paycheck") — and, per §2.0, the *deploy* it implies is **dampened by the discovery buffer**, not just hedged in copy.
- **Advice boundary (F-trust #7):** a one-time *"guidance from your numbers, not financial advice"* at first premium use **+ a persistent light "your call" affordance on the decisive per-cycle plan** (rides the existing mark-when-ready). **Classifier:** allocation calls with a genuine finance tradeoff (liquidity-vs-APR, EF-vs-debt) are always **two-sided-with-a-why**; only mechanical no-tradeoff moves get the single decisive voice.

### 2.2 The data model (the honest core)
- **Exhaustive partition — the canonical N-bucket list (F3 + round-4 §2.0).** The engine today emits **"Keep cash buffer"** and **"Leftover cash"** under one `leftover` category, so `selectLiquidCushion` sums both → "$700 cushion" when only $200 is protected. **Fix: split into the ONE canonical bucket set** that every selector, the bar, and the reconciliation test derive from — no other list may exist:
  1. `cushion_buffer` — the reserved floor. **= the REALIZED reservation `min(floor, discretionary_after_obligations)`, NOT the constant $200** (round-5 mechanics F2: the constant breaks the sum in the tight case — discretionary $120, floor $200 → the plan keeps $120, so bucket 1 = $120 — and in the shortfall case → bucket 1 = 0. Matches today's `min(paycheckBuffer, remaining)` at `allocatePaycheck.ts:299`. The `floor` *subtrahend* in §2.0.b's deploy formula is the floor **parameter**; this **bucket** is the clamp.)
  2. `prefunded_reserve` — cash held this cycle for a specific future crunch (§2.5).
  3. `discovery_holdback` — the §2.0.b uncertainty reserve (discovery + cold-start, composed by `max`).
  4. `starter_emergency` — the gated starter EF (§2.5).
  5. `emergency` — the fuller EF.
  6. `snowball` — debt (minimum + accelerate).
  7. `optional_goal` — goals.
  8. `true_leftover` — genuine residual liquid cash.
  - **`cushion` (the displayed "protected") = buckets 1+2+3+8** — round 4: the held reserves (2+3) must count as protected or "put to work" counts them as deployed (the lie inverted); round-5 mechanics F3: **`true_leftover` (8) also folds to the protected/kept side** — residual liquid cash is retained cushion, not "put to work" (matches today's `selectLiquidCushion` summing the whole `leftover` category), so the two zones sum cleanly. The two-zone bar's **"put to work" = buckets 4–7** (actual EF/debt/goals); the held reserves render as a **named zone inside the protected side** (§2.0.c).
  - Reconciliation test asserts `sum(all 8 buckets) === discretionary` to **±$0.01 per allocation line**; the **tight case** (bucket 1 = `min(floor, discretionary)`) and the **shortfall case** (`discretionary` clamped to 0 → all 8 buckets 0) explicitly asserted; and `cushion === buckets 1+2+3+8`. **[BUILD] the category→bucket table + all four category-summing selector updates + the bar-zone mapping land together in 2.4.6.1, all deriving from this one list.**
- **One metric, one state machine — ACROSS ALL THREE PRODUCERS (F4).** Code has *three* state producers: the card (`buildGuardianBrief`), the forecast (`toCushionStatus(endingBalance)`), and `selectPlanSummary` (its own `paycheckAmount*0.1` threshold). **Fix: one shared `computeState(discretionary, floor, priorBand)`; migrate all three call sites; `TimelineCycle` gains per-cycle `discretionary`; the lookahead reads floor-relative headroom, never `endingBalance`.**
- **Hysteresis — concrete (F4).** `computeState` takes the **prior band**; **enter-tight at `discretionary < floor`, exit-tight only at `discretionary > floor + BAND`** with **`BAND = $50`** ([BUILD] tunable, Phase 6). The **at-risk** boundary is **floor-relative** (`discretionary < floor × 0.5`, replacing the hardcoded `<100`), with its own hysteresis. Shortfall dead-band: ignore `shortfall < $5`. The prior band is **persisted** and **reloaded across rollover**; **[BUILD] define the missed-rollover case** (a stale "tight" prior must not suppress a legitimate "clear").
- **Living-expense reserve — LEAVE AS IS (F7, reversing round 2).** The field is **already per-paycheck by design** (UI: "set aside each paycheck"); the engine summing it raw per cycle is correct. **Do NOT add a "cadence" migration** — it would re-read every existing amount as monthly and divide by 2–4× (a false-clear for the install base). If a cadence field is ever added, it defaults to **per-paycheck** and preserves amounts verbatim.
- **Card (D1.2 → two-zone, hard-capped):** state line · two-zone bar (cushion + floor line vs put-to-work) · one read sentence · ≤3 plan steps · one control. The hero's Required/Everyday/Free bar and the Guardian bar are a **zoom-in** (obligation split → discretionary sub-split), not two peer bars. Scorecard → drill-down.

### 2.3 Variable / irregular income (v1.7)
- **Plan for the valley (D3.1);** projected cycles allocate on **`lean`** (the valley reaches the forecast, F3-r2/correctness #3).
- **Income-modelability gate (round-3 red-team #4 → v4 reframe → round-5 #1 three-way branch):** the `income-modelability` signal feeding §2.0.b's degrade routes to **three distinct states**, not one generic "too variable" (v4 lumped seasonal into the generic degrade — round-5 framing #1 caught that it discards the very signal the classifier detected and promises a convergence that never comes):
  - **Seasonal** (a month-of-year signal): commit to the pattern — *"I can see your income is seasonal — I'll hold steady through your lean months and deploy hard when your big months land."*
  - **High-variance-but-modelable** (high CoV, no seasonal signal): keep planning, **wider band + higher shrinkage**, no blame.
  - **Genuinely floorless** (no lean estimable): this-paycheck-only, framed as a deliberate protection state (§2.0.b).
  Full copy + gate mechanics in §2.0.b. (No combined `readConfidence` scalar — dropped in v4.)
- **Capture (D3.2):** varies-toggle surfaced **after a logged paycheck differs** from entered (not first-run); lean (required, *"the floor you can count on"*) + typical (optional). Relate the two floors: **lean income feeds the plan; the cushion line is protected within it** (different numbers, copy says so). **[BUILD] toggle-transition re-anchors drift + re-projects (§3.3).**
- **Learning — CORRECTED low-N (F6).** Below **N=12** actuals, lean = a **shrinkage blend toward `typical`** (a fixed haircut, [BUILD] default 15% under typical), NOT a percentile (a 10–15th percentile of 4–6 points ≈ the raw min it's meant to avoid). At **N≥12**, transition to a defined **low quantile (12th percentile, linear interpolation)**. The **up-nudge** ("you've reliably cleared $X — trust more?") is a **separate explicit rule** with precedence over the down-path; **outlier-guard** the actuals (a single wild entry can't move lean; a large lean shift requires confirm — round-3 red-team #8). Suggest-and-confirm, never silent.
- **Debt-free date — one engine, TWO runs, a BAND (cross-cutting F).** Reconcile the *three* current producers (state-threaded forecast, `selectDebtFreeDate`'s `projectDebtPayoff`, aspirational typical) into **one projection engine run twice** — on **lean** and on **typical**. Show **the typical (motivational) date as the headline**, lean as the "safe-floor" secondary — not a bare symmetric range (which invites anchoring on the rosy end and inverts intuitively). The **interest-cost-of-caution is rounded/qualitative** (*"somewhat more interest"* or a rounded band), never a false-precise `$X`, until the state-threaded forecast is proven (round-3 trust #6).

### 2.4 The acting — auto-protect the cushion floor (built, 2.4.6)
- Premium reserves the **cushion floor** (default $200, adjustable) as the paycheck buffer; only discretionary money moves; obligations never cut; spending user-executed. **Deploy above the floor is now governed by §2.0** (discovery buffer / cold-start / staleness). **Floor set below a data-derived safe minimum → a one-time honest warning (not a lock); toggling varies→fixed for a demonstrably-variable earner surfaces the ranged-income caution** (round-3 red-team #5).

### 2.5 The cash-flow brain (2.4.7)
- **State-threaded forecast (C5).** `applyRolloverPayment` **exists**; the projection loop must, per cycle, **allocate (on lean for variable income) → apply minimum+snowball to reduce each debt's balance → advance each goal's `currentAmount` → roll due dates → filter**. A retired debt frees its minimum into the next cycle's `discretionary` (verified sound). **[BUILD] the exact per-iteration order + which allocation-order version the projection uses is pinned in 2.4.D with reconciliation tests.**
- **Recovery / smoothing — REWRITTEN (round-5 mechanics F1: the v4 two-pass traded the false-clear for a false-ALARM).** The v4 aggressive-deploy detector stripped every prior cycle's surplus and pre-funded from cycle 0 only, so an **intermediate flush cycle couldn't smooth a later crunch** → a manufactured "un-smoothable deficit" (a cried-wolf risk push) on the common *flush-cycle-then-lumpy-bill* shape (quarterly insurance, property tax, annual renewal). **The correct model is neither the no-deploy accumulation (false-clear) nor the aggressive strip (false-alarm) — it's a whole-horizon fixed point with backward water-filling:**
  - **Carryover base:** `bal_0` = the **protected/retained** balance (`cushion_buffer`, the floor you keep). Per-cycle **un-clamped** running balance `bal_k = bal_{k−1} + net_k` where `net_k = income_k − required_k − living_k` — **negative carryover is preserved, never `max(0,·)`-clamped** (the forecast today clamps at `buildTimelineItems.ts:159`, losing the crunch magnitude `min_k` needs; the reconciliation test asserts the un-clamped value).
  - **Detect (whole-horizon):** identify each **crunch segment** — a maximal run of cycles where the running balance would dip below `floor` — and its **trough deficit** `= floor − min_{k∈segment}(bal_k)`.
  - **Backward water-fill (source from the nearest preceding surplus, not always cycle 0):** for each crunch segment, fund its trough deficit from the **nearest preceding cycle(s) with surplus above the floor**, walking backward until the deficit is covered. A cycle's sparable surplus = its running balance above `floor` *before* that cycle's own deploy. Only the shortfall that **no preceding surplus in the horizon can cover** is a genuine **structural deficit** (described honestly — no faked set-aside).
  - **Deploy cap is per-segment, not global:** each cycle's deploy is reduced only by the reserve *it* is asked to hold for a *downstream* crunch — **not** by a single global `min_k(bal_k) − floor` (which needlessly zeroes cycle-0 deploy for a deficit an intermediate cycle covers). `prefundedReserve` for cycle 0 = only what cycle 0 must hold for the nearest crunch its surplus is the closest source for.
  - **Fixed point:** project the whole horizon under the *same* smoothing/pre-funding policy (not one extreme). Because holding cash back only *raises* every later `bal_k` monotonically, the fixed point **converges without oscillation** and pre-funding can never *create* a new crunch. **[BUILD] the water-fill iteration + tie-break when two crunches compete for one surplus cycle, pinned in 2.4.7 with reconciliation tests** (the reconciliation suite asserts: no false-alarm on flush-then-bill, no false-clear on deploy-masks-crunch, un-clamped carryover, per-segment cap). *(This is the 3rd smoothing re-spec — its correctness is established by these tests when 2.4.7 is built.)*
- **Surplus waterfall — re-architecture with categories, and pre-fund as an INPUT not a rung (F2).** Order: **floor → [pre-funded reserve, injected as an *input* to the per-cycle allocator from the multi-cycle smoothing layer — NOT a rung inside single-cycle `allocatePaycheck`] → starter EF (gated) → highest-priority debt → fuller EF/goals → accelerate → wealth.** New categories: `prefunded_reserve`, `starter_emergency` (vs existing `emergency` for fuller). **"Highest-priority debt" follows the user's strategy** (snowball = smallest balance, avalanche = highest APR) — *not* a hardcoded "high-APR" (which contradicts snowball mode). **[BUILD] the starter-vs-fuller-EF split value + the D5.3 gate's stored answer + selector updates land in 2.4.7 with reconciliation.**
- **Deploy-amount safety:** the named deploy target/amount is derived from the **actual** snowball allocation (not a re-rank), guarded so min-only-projected balances can't name/overstate a debt.
- **Posture:** auto-apply protective; create-goal/deploy = one-tap confirm; obligations never cut.

### 2.6 Surfaces
- **Card:** §2.2 (hard-capped, calm register — one quiet fill, no count-up/haptic).
- **Drill-down (D5.2) — a route with mechanics:** deep-link into the elevated Progress cushion view with params forcing `segment=Cushion`, scroll-to + focus, a **Guardian-sourced header** the standalone visitor doesn't get, and an **announced** context switch; **a pushed-modal route is preferred over a silent tab-jump** (preserves "came from Today" + back). Forecast draws the **floor line across it** (shared token set + line treatment with the card — F-UX #4), crunch highlighted, per-cycle "why" (reuse `TimelineLedger`), calm motion matching the card.
- **Accessibility — a contract for EVERY new piece:** the adjust-line control, the plan-step summary, and the scorecard render **outside** the narrated `groupLabel` (the collapse recurs otherwise); the bar has a real `accessibilityValue`; `announce()` on floor change; forecast authored as readable per-cycle text; plan steps styled **non-interactive**; state redundant (icon + title, not color only); high-contrast verified.

### 2.7 Graduation — debt-free (D4.1)
- The Guardian **persists** past debt-free + re-tops (debt drops → EF → goals → wealth); celebration; earned Freedom invite (**convenience, not "the accurate version"**). **Fix (F8): `selectPaydayGuardian` ≠ null at $0, AND a debt-free-aware copy branch** — deploy target = EF/goals/wealth, never mislabeled "cushion"; the scorecard + income-learning have a defined persists-past-debt state.

### 2.8 Proactive notification (v1.7)
- **A neutral prompt, not a verdict (F-trust #4).** The banner **under-claims relative to any reconciled state** — *"Time to check this paycheck"* (neutral), never *"looks tight"* (a verdict a reconcile-to-clear turns into cried-wolf). Any risk word is reserved for states **stable across the provisional→reconciled delta** (a confirmed structural deficit, not a projection blip). On open-after-reconcile-to-clear, **acknowledge it** ("good news — this one looks clear after all"). The hedged number appears **only in-app**; deep-links into the drill-down.
- **Fires on RISK with chronic-risk suppression + a frequency cap.** Escalate on **change** (new/worse crunch), AND a hard **max of N=2 risk pushes per rolling month** regardless of change (round-3 red-team #6 — a volatile-bad user's every-cycle "change" would otherwise fire weekly). User cadence control ("only shortfalls I can't cover" / "monthly digest").
- **Mechanism (F-completeness #3, resolved): schedule-time compute + provisional-schedule-then-reconcile**, with the honest acknowledgment that a **never-opened user gets the provisional read** (so the banner's neutrality matters most for them). Requires the **store-level `currentCycleNotifyState` + push-log** (§3.1 — NOT on the historical snapshot) to detect change and enforce the frequency cap. **[BUILD] verify reliability across rollover-while-backgrounded.** Built with the native batch (2.9); honest interim copy until it ships.

### 2.9 Calibration + scorecard (v1.7, reworked)
- **Internal (silent) calibration first;** proven accuracy (**"calibration-maturity" ≡ §2.0.a `lean-verification` — the SAME N≥4-actuals gate, one canonical signal, not a fifth axis**; round-5 buildability #2) releases the cold-start holdback. Unproven → the **day-one protection state (§2.0.d — "floor protected from today, learning privately")**, never an apology or a number. **The scorecard reads each prediction's stamped `predictedConfidenceContext` (§2.0.e) and excludes/down-weights low-confidence/provisional reads**, so it grades committed predictions, not deliberate caution (else it re-inherits the C1 "grades conservatism" flaw via the confidence layer).
- **Honest visible scorecard:** measures *"how often my read matched what you confirmed,"* with a plain **"what this proves"** line (so it's read as neither "kept you safe" nor "agreed with itself"); **false-clear and false-tight reported separately**; the **warn threshold is decoupled** from the displayed number. **Bad-record copy branches on the dominant error (F-trust #2):** a false-tight-heavy record → "I've been over-cautious, recalibrating"; a **false-clear-heavy record OWNS the miss** → *"I've under-warned — I've tightened my read"* (never softened to "over-cautious," the one direction we can't spin). **Fixed-income users** (no lean–typical band) score **obligation-timing / discretionary-headroom prediction vs. confirmed outcome**, not "cushion held" (which holds by construction) — F-trust #5. Gated **N≥4**, homed in the drill-down. **[BUILD] disturbed-cycle exclusion (§3.2).**

### 2.10 Free vs premium (value-led, D2.3)
- Free **always helps** — honest read + universal safe guidance when tight + the invitation; the personalized multi-cycle plan is the premium line. **Paywall sells floor-protected-from-day-one + earns-trust-as-it-learns** (§2.0.d), never proven precision; "watch" is always qualified **private/on-device**.
- **Priority-not-outcome promise (round-5 framing #2):** the day-one claim is a **first-claim guarantee**, not an outcome a tight converter can't get — *"your floor is always first in line before a dollar moves,"* NOT "you'll have your $200 from day one." A buyer who's genuinely short this paycheck must not meet a promise that just failed.
- **The tight day-one is its own proof of value:** for the short converter, deliver the single most useful move, not a dead card — *"you're $X short this paycheck; here's the smallest change that closes it."*
- **Pricing de-risk = a stronger confidence-independent month-one proof, NOT a trial/guarantee (Jason's call, round-5 framing #4).** Rather than a money-back window or free trial to cover a throttled first month, **month one must carry genuine standalone value that doesn't depend on the learning window**: (1) the **floor auto-protect** made visibly valuable from paycheck one (not "we're not doing much yet"), and (2) the **tight-day-one smallest-move** above. These are confidence-INDEPENDENT (they work before any accuracy is proven), so the just-converted user gets real value in the exact window the learning-gated features are still warming up. **Lead annual / Lifetime pricing** (§4) — justified by the day-one proof carrying month one, not by asking the buyer to wait out the learning window on faith. **[BUILD] the month-one proof must be demonstrably impressive in the demo/showcase state (§3.6) — this is the highest-bar item of the pricing resolution; if it can't be made to impress standalone, revisit the trial/guarantee options.**

### 2.11 Hardening (2.4.6.1, before 2.4.7)
- Partition split + categories + reconciliation test (§2.2) · `computeState` unified across all three producers + concrete hysteresis · at-risk floor-relative · focus from the actual allocation · no "$0" nonzero · windfall not repeated · VoiceOver-reachable adjust-line · hide adjust-in-shortfall · animate-once · framing reshapes · the §2.0 confidence-governance action/voice gates.

---

## 3. The Data Substrate (structure-first, v1.7 — `2.4.D`, first)

- **3.1 Schema (v4→v5) — re-derived from §2.0's real needs (round-4 completeness).**
  - `PaycheckConfig` += `incomeVaries`, `leanAmount`, `typicalAmount`, **`plannedAmountPerCycle`** (so §2.3 capture can diff planned-vs-actual — today `amount` is a single overwritten scalar with no history).
  - **Read-freshness stamp** (§2.0 read-freshness signal): a store-level **`inputsAsOf`** timestamp **stamped `= now` on every genuine income/bill/balance EDIT mutation** — and pointedly **NOT** by `applyRollover` (round-5 data B1: the v4 "derived from the newest edit" reading is *unbuildable* — `PaycheckConfig`/`RequiredExpense` carry no edit timestamp; and if rollover refreshed it, the staleness cutoff could never fire for an actively-rolling user). NOT per-debt `lastVerifiedDate`. §3.7 defines the reconciliation.
  - **Bill-completeness substrate** (§2.0 bill-completeness): an **onboarding stamp** + a **`genuineCycleCount`** that increments only on real lived cycles and **excludes seeded/imported/`disturbed` cycles** (round 4: `cycleHistory.length` is pre-loaded 6 by `demoSeed` and backfilled by import → a seeded user would skip the discovery buffer). Plus a **surprise-outflow log** — a write path that records when a reconciliation surfaces an un-modeled outflow (the second half of the completeness proxy, which today has no producer). **This needs a real capture surface (round-5 data B5): `applyCapture` takes only `items` + `requiredDecisions` today — add an "unexpected expense this paycheck?" affordance at the payday reconcile** so the signal has a producer, not just a field.
  - `PayCycleSnapshot` += prediction stamp (`predictedCushion`, `predictedState`, `predictedShortfall`, **`predictedConfidenceContext`** [§2.0.e: active holdbacks / provisional flag]) + outcome (`actualIncome`, `actualCushionHeld`, `outcomeConfirmed`, **`disturbed`**).
  - **Notification state moves to STORE level, off the snapshot** (round 4: snapshots are historical closing records; the §2.8 notification fires for the *current* cycle which has no snapshot row yet): a store-level **`currentCycleNotifyState`** (like the existing `lastHandledPaydayDate`) + a **push-timestamp log** for the "≤N=2 pushes / rolling month" cap (underivable from a per-cycle flag).
  - **Income-actuals log = per-paycheck granularity** (not per-cycle `actualIncome` — weekly earners have multiple paychecks/cycle), each paired with the `plannedAmountPerCycle` it's diffed against. **Exclude `store.windfall` from the logged actual** (round-5 data B3: one-time extra income cleared at rollover would inflate the lean — exclude it, or flag that paycheck's actual `disturbed` for learning; mirrors the §2.3 outlier-guard).
  - **Field types (round-5 data B7):** `leanAmount`/`typicalAmount`/`plannedAmountPerCycle` are **numbers**, parsed at the same engine boundary as the existing string `amount` — no mixed-type drift.
  - **Migration is a hard prerequisite (round-5 data B4):** bump `CURRENT_STORE_VERSION` 4→5 **and** add every new field to `createDefaultStore` first (`runMigrations` merges `...base, ...r`, so a field absent from defaults lands `undefined`) — `genuineCycleCount: 0`, empty logs, `inputsAsOf: currentDate`, `currentCycleNotifyState`, persisted prior band, `incomeVaries:false`. Backfill = safe/decisive defaults (fixed income, no hedge). *Accepted regression: an established upgrader re-enters the 3-cycle discovery holdback (round-5 data B6) — the safe/conservative direction; flag in release notes.*
  - Persisted **prior band** (§2.2 hysteresis).
- **3.2 Capture — a DEFINED trigger for EVERY entry path (F5 + round-4 completeness #5).** The prediction is stamped **via an explicit store mutation, never inside the pure selector** (the old "first Guardian compute" fallback was a forbidden selector side-effect). The trigger is an **entry-path matrix**, not just the rollover path:
  - **rollover** → stamp at the rollover that creates the cycle.
  - **onboarding mid-cycle** → a **cycle-detect mutation on app-open** stamps the first real cycle (a new user has had no rollover, so "created by a rollover" never fires for them).
  - **import** → backfilled snapshots are flagged `disturbed`/seeded and **do not** increment `genuineCycleCount` or feed calibration.
  - **demo (§3.6)** → fabricated predictions/outcomes/scorecard rows are isolated and **never** leak into a real user's calibration.
  - On a material mid-cycle change, **re-stamp AND flag `disturbed`**; disturbed cycles are excluded from the N≥4 calibration count. Actual reconciled at rollover (via `applyCapture`, which today logs neither). One pipeline feeds learning + calibration.
- **3.3 `shouldReAnchor` learns income — with the lean-nudge↔Drift decision (round-4 completeness #7).** Add the varies-toggle + a material lean change to the re-anchor set; define the fixed↔varies transition. **Decision: a *learning-driven* lean nudge (the app refined its income estimate) is a MEASUREMENT change → it preserves the frozen Drift baseline** (else "days ahead/behind" resets with no behavior change); **a *user-driven* income/floor edit is a PLAN change → it re-anchors.** Drift keeps measuring adherence to the plan the user committed to, not to the app's self-corrections. **Enforcement (round-5 data B2): thread a `source: 'learning' | 'user'` into the re-anchor decision** — today `shouldReAnchor` compares `monthlyExtraPayment` with no change-source (`computeDrift.ts:155-157`), so a learning nudge that lowers projected income would silently re-anchor. Either pass `source` through `recordDriftBaseline`'s call, or exclude learning-derived income from the compared value.
- **3.4 Forecast state-threading** — the §2.5 loop; the C5 blocker; reconciliation-tested.
- **3.5 Cold-start contract** — every learning/calibration surface has a defined empty/low-data state + min-N (learning shrinkage < N=12; scorecard number < N=4; hedge-hard confidence until then); ties to §2.0. Cold-start is 100% of week-one users.
- **3.6 Demo/showcase substrate (launch-critical)** — a scripted premium demo state (tight cycle + flush cycle + income-varies + populated actuals/scorecard), separate from the free explore-seed (which disables Autopilot). Decide: second seed vs. a demo "preview premium" toggle. Demo rows are isolated per §3.2 (never feed real calibration; never inflate `genuineCycleCount`).
- **3.7 §2.0 reconciliation seams (round-4 completeness — settle before 2.4.D).**
  - **Read-freshness vs per-debt staleness:** the §2.0 freshness gate reads store-level `inputsAsOf`, NOT per-debt `lastVerifiedDate`. Because 2.3 auto-maintenance + `applyRollover` keep balances current *without* advancing `lastVerifiedDate`, a rolled-over/auto-maintained debt must **not** trip the freshness hedge. The hard "I can't see far enough" cutoff fires only when `inputsAsOf` itself is genuinely stale (user hasn't touched real inputs past `ESTIMATE_STALE_DAYS`).
  - **Substrate is NOT debt-gated (round-4 completeness #6):** `selectPaydayGuardian` returns null at `liveDebts=0` today, which would stop stamping/calibration/income-learning exactly when §2.7 wants the Guardian to persist. **De-gate the substrate from debt-presence:** the prediction/outcome/learning pipeline keeps running debt-free, with the stamp target = **EF/goals headroom** (not "cushion held"). The §2.9 scorecard + income-learning have a defined persists-past-debt state.
- Reconciliation-tested throughout.

---

## 4. The rest of the premium tier
- **2.3 auto-maintenance (BUILT).** *v3:* the Guardian's staleness handling is the §2.0 gate, not a per-sentence hedge; reconciled with "always current."
- **2.5 smart-obligation — must ACT + bias to INCLUSION.** Auto-holds pending confirm (not a passive flag); classifier only ever cautions toward *inclusion* (never silently excludes → a dropped bill = a false-clear). **Ships with/before the Guardian, or interim Guardian copy is non-alarming on thin data** (min-only balances + a miscounted obligation both push toward false alarms in the launch window).
- **2.6 close-the-loop — sell the RECONCILE, not the nag.** Homes the Drift→Guardian contract + anchor unification + the calibration loop.
- **2.7 scan-to-prefill** — reduces entry (a partial answer to the manual-entry retention exposure the competitor teardown flagged). Fast-follow.
- **2.8 momentum — RETENTION, free layer; only custom share-art premium.** Never in the price justification.
- **2.9 widget + App Intents + notifications** — includes the §2.8 notification.
- **2.10 revenue spine** — RevenueCat + paywall (inherits the §2.10 framing: **first-claim floor promise + earns-trust-as-it-learns**, "watch" qualified private/on-device; **no "always-watching precision"**; **lead annual/Lifetime**, de-risked by the **stronger confidence-independent month-one proof** — floor auto-protect + tight-day-one smallest-move — NOT a trial/guarantee, Jason's round-5 call) + portfolio-sub + analytics/Sentry; launch-flip gated on value. **[DECISION] Lifetime-vs-portfolio-sub cannibalization** — define Lifetime's scope publicly + the graduate path.
- **2.11 E2EE iCloud + AU/NZ** — "E2EE" wording gated per §1.
- **~v1.8 Connected** — Plaid; convenience-not-accuracy; retention claims scoped to our backend, Plaid named. **Future: Ava.**

## 5. Cross-cutting principles
1. Private by default; "E2EE" only where verified true.
2. Honesty over polish — and **the honesty is enforced (§2.0), not just stated**: action + voice are gated by input-confidence.
3. One free/premium line ("free tells you, premium does it").
4. Auto-apply protective; confirm to spend/create; obligations never cut.
5. Confidence scales with proven accuracy — earn trust, don't assert it; **it acts on what it's sure of from day one (your floor) and does more as it learns you** — cold-start is a protection state, not an apology.
6. Removes work, not just info.
7. Watches + acts so you don't have to (the identity) — **positioned on the two durable claims** (engine-grounded honesty + automated payday-debt focus), the two decaying ones retired.
8. Distribution is the job too — ship the differentiated feature *visible*.

## 6. Build order
**2.4.D data substrate (schema + §3.2 capture + §3.4 state-threading)** → **2.4.6.1 hardening (partition split + `computeState` + confidence-gates)** → **2.4.7 cash-flow brain (running-balance smoothing + waterfall re-arch + valley-into-forecast + learning)** → **2.4.8 graduation** → **2.4.9 calibration + scorecard** → **2.4.10 notification** → **2.4.11 reshapes + demo showcase** → rest of the tier (2.5 with/before Guardian → 2.6 → 2.7 → 2.8 → 2.9 → 2.10 → 2.11). Connected ~v1.8; Ava future.

## 7. What is deliberately **[BUILD]-time** (so round 4 doesn't re-flag as omission)
These are pinned to **defaults** here but finalized in the structure-first build with reconciliation tests as the ratchet (a prose spec can't be as precise as code+tests): the exact projection **loop order**; the §2.5 **water-fill iteration + crunch-competition tie-break**; the category→bucket selector wiring; the hysteresis `BAND`, **discovery-buffer fraction (40%)**, **coldStartHoldback fraction (25%)**, the `max`-vs-`max+λ·min` holdback-compose choice, staleness cutoff, and CoV/seasonality thresholds (all real-use-tunable, Phase 6); the ADP-detection API's existence; notification reliability across backgrounded rollover. Everything **design-level** (the §2.0 layer with its per-signal gates + composition rule + water-filling smoothing, the partition/state-machine, positioning, the honesty + pricing policies) is settled here for the round-6 audit to verify.
