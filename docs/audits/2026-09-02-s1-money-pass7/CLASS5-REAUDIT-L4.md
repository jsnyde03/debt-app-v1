# Class 5 re-audit, round 1 -- lane L4 (the instruments)

- Lane: L4 -- check-finding-guards, check-trust-claims, test-gate-plants, run-gates --fast, registry entries the range added; class-4 tail R5-1 registry rows + R5-2 word-boundary matcher.
- Pin: `ea3f5e0e`. Range: `c7df99c2..ea3f5e0e`.
- Worktree: C:/Users/Jason/audit-c5r1-L4 (detached at pin). Base worktree for attribution: C:/Users/Jason/audit-c5r1-L4-base (c7df99c2), torn down at end.
- Worst-case spend quote: single agent, no sub-agents; ~3-4 hours wall clock of node/tsx runs (per-id prove:guards, test:app, lint scripts, gate plants), no Playwright, no whole-monorepo typecheck, heap 1536 MB; token spend est. <= 3M context tokens.
- **Spend re-quoted at 13:42:** under four-lane contention one `test:gate-plants` run takes ~20 min (measured: `S1P7-57-2D`'s planted run, 13:18->13:38). The read-only proof loop (33 ids, one at a time, `--no-record`) is left to run to completion; stopping it was not permitted. The plants run only after its `DONE`, so the worktree is never shared. The one combined gate-plants plant of the registry's own recorded un-fixes (`gp-A`) is kept as a cross-check and to re-measure `S1P7-57-2B-VACUOUS-CONJUNCT`, whose proof died on the environment (NM-8). Revised worst case from 13:42: ~5 h wall clock. Single agent, no sub-agents, no Playwright.

Status: COMPLETE

## Findings

### F1 · `MAX_SERVER_ATTEMPTS`'s arithmetic is computed over 17 Playwright proofs; the range made it 24 · minor · attributable

- **Consequence:** the [D78] retry cap's written justification (`k=6 -> ~6%` chance a full `--all` hits an unrecoverable kill) no longer describes the registry. At 24 proofs, `1-(1-2p^6)^24` with p=1/3 is ~6.4% vs the ~5.9% the comment derives for 17. The number is not badly wrong. But the comment says it is written down *"so the next person can re-derive it instead of trusting it"*, and its input is now stale. It is the "edit the value, leave the sentence" shape (`V10`), here "add the population, leave the derivation".
- **Where:** `scripts/prove-guards.ts:240-249` (`**17** playwright-backed proofs in the registry`). The file is not in the range; the population it describes is.
- **Measurement:** Playwright-backed proofs (`cmd` containing `playwright` or `run: test:e2e*`), counted from `git show <sha>:scripts/finding-guards.json`: **17 at `c7df99c2`, 24 at `ea3f5e0e`.** The 7 added are `S1P7-57-3-FREE-C3-8-TWIN`, `-AFFORDABILITY-REFUSAL`, `-WINDFALL-REFUSAL`, `S1P7-57-4B-RUNWAY-HELD`, `-CONFIRM-FUNDED`, `S1P7-57-5-HERO-FIT-WEB`, `-HERO-ALL-MONTHS`. The brief's own "24" agrees.
- **Reproduces at `c7df99c2`: NO.** At base the comment's 17 was exact.
- **Mechanism (HYPOTHESIS):** registering proofs moves `MIN_ENTRIES`, whose prose is kept per round (NM-5). Nothing ties the retry-cap derivation to the population it reasons over.
- **Remedy (UNVERIFIED):** re-derive at 24 in the comment, or state the derivation as a function of n and print n at run time. Either stays a prose fix; no proof proposed (`authored` is 9 of cap 9).

### F2 · eight class-5 closures carry no registry row, and no instrument maps pass-7 closures to rows · minor · attributable

- **Consequence:** `[D67]` says *"a closed finding needs a standing guard, or it is not closed"*, and `lint:finding-guards` is the deletion detector for that guard. For `C3-13` `C3-9` `C3-11` `C1-1` `C1-5` `C1-6` (table blockers) and the plan's `C1-2` `C1-3`, no registry entry names the pass-7 closure. Deleting the assertion that closed one leaves every static gate green and nothing stale: staleness is keyed on `proof.unfix[].at`, and no proof exists to key on. `MAX_UNPROVEN` counts entries, not closures, so a closure that never becomes an entry is not a count the ratchet can see.
- **Where:** `scripts/finding-guards.json` (28 entries added in range; none for these ids). `scripts/check-audit-closure.ts:20,58`: `lint:closure` reads only `2026-08-17-v1.7-audit-gate/findings` and `2026-08-21-p6.8-finish/slices`, never pass 7.
- **Measurement:** word-boundary search of every pin entry's id and `what` for each id. The hits for C1-1/C1-6 are pass-6/pass-4 entries (`S1P6-C1-1-NAMETHEFIGURE`, `S1P6-C1-6-AMOUNTCOLLAPSE`, `S1P4-C4-7-SURFACES`); C3-13/C3-11 are named only by instrument entries (`S1P7-57-2D-DEBT-SPREAD-LEDGER`, `S1P7-57-2B-*`), which prove the instruments, not the closures. Labelled assertions do exist for C3-13, C3-9, C1-2, C1-3, and the plan records C3-11/C1-5 plants at close. So these were planted when fixed and are **executed by `test:app`/e2e**, but **deletion-undetected**.
- **Reproduces at `c7df99c2`: cannot be measured there.** The closures did not exist; argued from the diff. The same shape (a closure left off the registry) is prior practice only where a written `unguarded` reason exists, and these have none.
- **Mechanism (HYPOTHESIS):** registration was batched at `.5.7 ⑦` against `MAX_AUTHORED` 9 of 9. The batch took the render proofs `.5.7` still owed and not the `.5.2`-`.5.4` closures, which already had tests.
- **Remedy (UNVERIFIED):** register one entry per closure (token on the closing assertion, `proof` from the recorded plant), or a written `unguarded` reason per id. ⚠️ Any new proof re-enters the authored deadlock (9 of cap 9); drain first. Not re-filed if the driver finds it already scheduled. I found no backlog row for it (`DEBT_ELEVATION_BACKLOG.md` searched for each id with registr/entry/owed/guard/proof).

### F3 · [D81] "a full run refuses to skip" is keyed on the same `FAST` that selects the skip, and nothing guards either · minor · attributable

- **Consequence:** the refusal can only fire when selection disagrees with `FAST`. A skip that comes in THROUGH `FAST` passes it by construction. The measured case is a `FAST` that is true without the flag: then CI's plain `npm run lint:rn` (`web-e2e.yml:92`) and `validate:release:rn` silently run 47 of 52 gates, skip all five self-tests (`test:gate-plants` included, the only thing proving 30 gates fail closed), and exit 0. The only difference is a `NOT a full run` line nobody reads in CI. Deleting the refusal outright is equally silent. It is "a check whose two sides come from one source", in the file that claims to be the guard against a quietly skipping runner.
- **Where:** `scripts/run-gates.ts:183-193` (`const FAST = process.argv.includes('--fast')`; `if (!FAST && selected.length !== GATES.length)`).
- **Measurement** (probe `plant.py`, outputs in `plant-runs/`; every restore byte-verified, tree clean after each):
  - *control, the refusal CAN fire:* `d81-control-selection-skips` (selection filtered in full mode) makes plain `run-gates.ts --list` exit **1**: `full mode selected 47 of 52 gates, so the runner is skipping: ...`.
  - `d81-fast-unconditional` (`FAST = process.argv.includes('--fast') || true`): plain `--list` exits **0** and prints `lint:rn --fast: 47 of 52 gates would run`; the refusal is silent. Over the same plant `lint:runner-completeness` 0, `lint:ci-chain` 0, `lint:finding-guards` 0 (329 of 330, unchanged).
  - `d81-delete-refusal` (the whole `if` block removed): `lint:runner-completeness` 0, `lint:ci-chain` 0, `lint:finding-guards` 0.
  - Registry: no entry's token or un-fix names the refusal, `FAST` or `isSelfTest` (grep of `finding-guards.json`, `test-gate-plants.ts`, `check-runner-completeness.ts`, `check-ci-chain.ts`, `test-plant-safety.ts`: 0 hits).
  - Baselines for all three gates were green on the clean tree.
- **Reproduces at `c7df99c2`: cannot be measured there.** `FAST` and the refusal were added by `d559e4dd`, inside the range. At base, `for (const gate of GATES)` had no selection to subvert. Argued from the diff: the one-source shape entered with the flag.
- **Mechanism (HYPOTHESIS):** the refusal was written to catch an edit to the `selected` expression. It compares `selected` against `GATES` under the same flag that produced `selected`, so it cannot see an edit to how the flag is derived.
- **Remedy (UNVERIFIED):** judge the full-run claim from an input independent of `FAST`. For example, have `lint:ci-chain` / `check-runner-completeness` assert that `run-gates.ts` derives `FAST` only from the literal argv flag; or, when `process.env.CI` is set, refuse unless the run's own final summary is the full-mode line. ⚠️ A registry proof for this re-enters the authored deadlock (9 of cap 9). Say so before adding one.

### F4 · `S1P7-B1-1-SAVEFORIT-PACE-KEPT`: the token pins the DATE assertion and the recorded proof reds on the PACE assertion. The pace assertions delete silently, and the next re-proof reads WRONG · minor · attributable

- **Consequence:** the entry's `token` (`is ready in ${o.paychecks} paychecks at the rate the engine funds`, `affordability.test.ts:173`) and its `expect` (`B1-1 — one debt, nothing held: "Save fast" promises`, the interpolated label of `:171`) are two DIFFERENT assertions in one loop. So the assertion the recorded proof redded on (`:171`), and its sibling `:172` ("the pace is kept"), can both be deleted with every static signal green. `lint:finding-guards` still finds the token. Nothing goes stale, because staleness is keyed on the un-fix target `guardianSelectors.ts`, not the test file. The ledger keeps `measured`/`sha`, describing a red that can no longer occur. The closure itself still reds on the recorded un-fix, through the date assertion. The first signal is a `reason=WRONG` on the next re-proof, read as "the guard redded, but not for B1-1".
- **Where:** `scripts/finding-guards.json` entry `S1P7-B1-1-SAVEFORIT-PACE-KEPT`. `apps/rn/src/store/affordability.test.ts:171-173`.
- **Measurement** (`plant.py`; `plant-runs/b11-*`; restores byte-verified, tree clean):
  - *see control:* `b11-see-delete-date-assert` (line 173 deleted) makes `lint:finding-guards` exit **1**: `S1P7-B1-1-SAVEFORIT-PACE-KEPT — the guard is gone ...`. The gate matches this `${`-bearing token, so the brief's "stop before `${`" rule is violated here without making the guard unfindable.
  - `b11-delete-pace-asserts` (lines 171 and 172 deleted): `lint:finding-guards` **0** (329 of 330, unchanged); `test:app` **0**.
  - `b11-delete-pace-asserts-plus-unfix` (same deletion, plus the entry's own recorded un-fix on `guardianSelectors.ts`): `test:app` **1**, on `FAIL [⛔ B1-1 — a held expense reserve: "Save fast" is ready in 3 paychecks ... the date is kept]`. The recorded `expect` `"Save fast" promises` is absent, so a `prove:guards` re-run would score `wrong-reason`.
  - With the tree unplanted, the same proof scored MATCHED (NM-12).
- **Reproduces at `c7df99c2`: cannot be measured there.** The entry and both assertions were added in the range (`ecc1e661` registered it, `.5.5`). Argued from the diff.
- **Mechanism (HYPOTHESIS):** the `R3-3` borrow check asks only whether `expect` sits in ANOTHER entry's token. Nothing asks whether `expect` and `token` name the same assertion. The token was moved to the date line (probably to satisfy the declaration/`${` rules), while `expect` kept the first red.
- **Remedy (UNVERIFIED):** point `token` and `expect` at the same assertion. Either the pace line `:172` with a token that stops before its first `${`, or `expect` at the date label. Re-run the proof either way. ⚠️ A fire-count of a general "token and expect on one assertion" rule is not proposed: pass-5 measured that shape heuristics red 30-84 legitimate entries.

### F5 · `MAX_DEBT_SPREAD_SITES` is a ceiling, not a pin, so a drained ledger leaves slack a new spread walks into without touching the cap · minor · attributable (liveness twin: reservoir)

- **Consequence:** the ledger docblock says the cap is *"Downward-only, and a LITERAL"*, and on a removed row the gate says *"Remove the row and lower MAX_DEBT_SPREAD_SITES"*. Nothing enforces the second half. The comparison is `debtSpreadTotal > MAX_DEBT_SPREAD_SITES`, so once a site is gone the cap stays at 10 over a ledger of 9. The next copy of a projected debt then needs only a ledger row, and the cap, the one number meant to make growth a deliberate act, never moves. This is the registry's own argument for strict equality (*"a cap above its own count is slack the next un-evidenced entry hides in"*, `check-finding-guards.ts:661-662`). And the gate's subject is `C3-13`: a spread of a projected debt reopens a blocker with every test green.
- **Where:** `scripts/check-trust-claims.ts` (`const MAX_DEBT_SPREAD_SITES = 10;` and `if (debtSpreadTotal > MAX_DEBT_SPREAD_SITES)`, in the `.5.7 ②` block).
- **Measurement** (`plant.py`; restores byte-verified, tree clean):
  - `spread-cap-slack`: one real site removed (`sandboxScenarios.ts` `{ ...d` -> `{ ...(d)`, which the gate's regex no longer counts) and its ledger row deleted, cap left at 10. `lint:trust-claims` exits **0**, ledger 9 against cap 10, and prints no word about the cap.
  - *control, the ledger itself is exact:* `spread-exact-control` (a second spread in the same file, row unchanged) exits **1**: `ledgered at 1 spread(s) and holds 2`.
  - Baseline green.
- **Reproduces at `c7df99c2`: cannot be measured there.** The debt-spread ledger was added at `48aa6697`, in the range. ⚠️ **Its twin predates the class:** `MAX_LIVENESS_SITES` is compared the same way (`livenessTotal > MAX_LIVENESS_SITES`, `git show c7df99c2:scripts/check-trust-claims.ts:515`). That copy is **reservoir**; this one is a new instance, written in the range.
- **Mechanism (HYPOTHESIS):** the new ledger copied the liveness ledger's shape, including the one-sided cap. `lint:cap-literals` checks that caps are literals, not how they are compared.
- **Remedy (UNVERIFIED):** compare with `!==` (red above and below, the `MIN_ENTRIES` / `MAX_UNGUARDED` idiom), for this ledger and its liveness twin. Fire-count at the pin: 0 for both (ledger totals 10 = cap 10, 22 = cap 22, from the gate's own green output).

### F6 · the two `.5.7 ②` trust-claims scans are proven in ONE direction each: the ledger's exact-count and dropped-row branches, and the reversed conjunct order, delete with every gate green · minor · attributable

- **Consequence:** `[debt-spread]` claims *"Exact in BOTH directions"*, and `[vacuous-conjunct]` claims to refuse a contained conjunction *"where A's fields are a subset of B's, or B's of A's"*. The only standing proof of either is one `test:gate-plants` scenario, and each scenario plants a single direction: an off-ledger file, and the inner-claim-first order. Deleting the exact-count check, the dropped-row check, or the reversed-order half of `covers(b, a) || covers(a, b)` leaves `lint:trust-claims`, `test:gate-plants` and (for want of a registry entry naming them) `lint:finding-guards` green. After such a deletion, a ledgered file can gain a projected-debt spread silently (reopening `C3-13`'s class), and `mayClaim(s,'row-figures') && mayClaim(s,'required-plan')` ships as the no-op `.5.3` shipped. It is the "plant both directions" rule in the gates written in this range.
- **Where:** `scripts/check-trust-claims.ts`, the `[debt-spread]` loop (`} else if (row.sites !== n) {` and `if (!debtSpreadCounts.has(rel)) failures.push(`) and the `[vacuous-conjunct]` loop (`if (covers(b, a) || covers(a, b)) {`). `scripts/test-gate-plants.ts`, the two scenarios' single `edit` plants.
- **Measurement** (`plant.py`, `plant-runs/`; restores byte-verified, tree clean):
  - *controls, each direction is live today:* `spread-exact-control` exits **1** (`ledgered at 1 spread(s) and holds 2`). `vac-reversed-control` exits **1** (`[vacuous-conjunct] ... drift.ts:71 — mayClaim('row-figures') && mayClaim('required-plan')`).
  - `spread-exact-unfixed` (the same extra spread, exact-count branch disabled): `lint:trust-claims` **0**.
  - `vac-reversed-halfunfix` (the reversed plant, with `covers(b, a)` only): `lint:trust-claims` **1**, but only on the two `[floor]` consumer-count lines. `grep -c vacuous-conjunct` = **0**, so the refusal is gone and the file reds for an unrelated reason that any real conjunction on an already-counted consumer would not trigger.
  - `gp-B` (one `test:gate-plants` run with all three deletions on disk; clean baseline 30 of 30): `✅ lint:trust-claims [debt-spread] ... control=exit 0 · reason=MATCHED` and `✅ lint:trust-claims [vacuous-conjunct] ... control=exit 0 · reason=MATCHED`. The standing proofs stay green over all three.
- **Reproduces at `c7df99c2`: cannot be measured there.** Both scans were added at `48aa6697` in the range. Argued from the diff.
- **Mechanism (HYPOTHESIS):** each scenario was written to red the named defect once, and `verdict()` asks "did this plant red for its reason", not "is every branch the claim names exercised". The same shape class 4 recorded for `[R3-3-borrow]` vs `R5-2`: *coverage of a rule is not coverage of its matcher*.
- **Remedy (UNVERIFIED):** add the second direction to each scenario's plant (a ledgered file gaining a spread; the outer-claim-first order), or a module-scope self-check over `covers()` in both orders. ⚠️ A new scenario raises `MIN_SCENARIOS` only. A new registry proof re-enters the authored deadlock (9 of cap 9).

### F5b · `MAX_LIVENESS_SITES` has F5's one-sided comparison · minor · reservoir

- **Consequence / mechanism:** same as F5, on the older liveness ledger (22 sites, cap 22): `livenessTotal > MAX_LIVENESS_SITES`, so a removed liveness site leaves slack for a new one.
- **Where:** `scripts/check-trust-claims.ts`, the liveness block (`:515` at `c7df99c2`). **Not changed in the range.**
- **Measurement:** read at base via `git show c7df99c2:scripts/check-trust-claims.ts` (`:493` literal 22, `:515` `>` comparison). The debt-spread twin was measured by plant (F5 `spread-cap-slack`, exit 0 at 9 of cap 10); the liveness copy was not separately planted.
- **Reproduces at `c7df99c2`: YES** (by reading, the line is byte-identical there). Routes to its own class; recorded as evidence beside F5 rather than as a class-5 defect.
- **Remedy (UNVERIFIED):** as F5.

## Measured non-defects (with controls)

- **NM-1 · baselines at the pin are green** (worktree `audit-c5r1-L4`, clean, HEAD `ea3f5e0e`, 3 junctions verified): `lint:finding-guards` exit 0 (330 entries, 201 EXECUTED, 2 stale = the known `S1-ROUTE-*` pair, 9/9 authored); `lint:trust-claims` exit 0; `lint:cap-literals` exit 0 (30 caps); `lint:runner-completeness` exit 0 (test:app 87/87); `test:app` exit 0 in 51 s. Outputs: scratchpad `base-*.txt`.
- **NM-2 · `run-gates --list`**: `--fast --list` selects 47 of 52 and names the 5 skips (`prove:guards:selftest · test:gate-plants · test:wrap-escapes · test:plant-safety · test:joined-code`); plain `--list` selects 52 of 52. The only CI consumer (`web-e2e.yml:92`) and `validate:release:rn` both run plain `lint:rn`; `gate:record` writes only under `--from-gate` inside `validate:release:rn`. No chain passes `--fast`. `npm run lint:rn --fast` (no `--`) is consumed by npm, so it runs FULL -- the safe direction.
- **NM-3 · the range's 28 added registry entries, shape** (probe `added_entries_shape.py` -> `.out.txt`): every `run` is a real npm script (`test:app` / `test:gate-plants`), every `cmd` is an argv list, none carries both; every anchor matches exactly 1x; no expect is a borrow (0 lenders). One token contains `${`: `S1P7-B1-1-SAVEFORIT-PACE-KEPT` -- see F-candidates.
- **NM-4 · `R5-2` word-boundary matcher, fire-count at the pin** (same probe): 330 ids, 0 outside `[A-Za-z0-9-]`, 0 empty short forms (the injection premise still holds at 330); 4 entries enter the borrow branch, all 4 waived under BOTH matchers (0 newly refused); **0 short forms shared by two full ids** (so no equal-short collision lets a note naming one entry waive another); 20 of 330 short ids are a prefix of another (the collision class R5-2 closes). Already filed and not re-filed: `R5-2`'s registry row is owed (`DEBT_ELEVATION_BACKLOG.md:1248`).
- **NM-5 · `MIN_ENTRIES` prose vs count**: the per-step lines 302->305->306->308->309->310->311->312->316->330 sum to the 28 added ids, each step's named members match the added ids. `MIN_SCENARIOS` 27 + 3 = 30 matches its comment.
- **NM-6 · runner orphans**: the range added exactly one test file (`liveActivitySync.test.ts`); it is wired in `runAppTests.ts:198` and default-exports its async runner (`:101`), so `.default()` is not a no-op. All other range test changes are edits to already-wired files.
- **NM-7 · the vacuous-conjunct lattice** (probe `lattice_probe.ts`, recomputing the gate's own `claimCover`/`covers` over the pin modules): 12 contained pairs, including the plant's `required-plan ⊑ row-figures`; the one production conjunction (`widget/snapshot.ts:245`, `debt-balances && solved-projection`) is NOT contained in either direction (`originalBalance`) -- the gate's comment holds. Note: an unknown claim name is contained in everything as the inner half (`covers(x, 'no-such-claim') === true`), which reds -- the safe direction.
- **NM-8 · environmental incident, not a verdict**: the first read-only proof (`S1P7-57-2B-VACUOUS-CONJUNCT`, `--no-record`) died in `prove-guards.ts:482` `gitStatus` -- the post-restore `git status` spawn exited `3221225794` (0xC0000142, STATUS_DLL_INIT_FAILED) while four lanes ran processes concurrently. The byte-restore assertion (`:478-480`) had already passed, the next proof's pre-flight accepted the target as clean, and `prove-guards.ts` is outside the range. Re-run, not counted.
- **NM-9 · `S1P7-57-2D-DEBT-SPREAD-LEDGER` re-executed at the pin** (`prove-runs/S1P7-57-2D-DEBT-SPREAD-LEDGER.txt`, `--no-record`): `plant-applied=YES · planted=exit 1 · control=exit 0 · reason=MATCHED`. ⚠️ Its `expect` is the scenario LABEL (`lint:trust-claims [debt-spread]`), which the green run prints too, as a ✅ line. It still attributes because `lib/verdict.ts` matches only lines the plant INTRODUCED, and the ✅ and ❌ forms of that row differ after normalisation. The planted run red because this scenario flipped. It is the D5-5 mechanism, and it holds here.
- **NM-10 · `S1P7-57-2A-PROJECTED-STALENESS` re-executed at the pin** (`prove-runs/S1P7-57-2A-PROJECTED-STALENESS.txt`, `--no-record`). Its recorded un-fix is `if (false && PROJECTED && stale + projected > MAX_STALE_PROOFS)`, and it scored `plant-applied=YES · planted=exit 1 · control=exit 0 · reason=MATCHED`. The `--projected` refusal is live for the direction the registry names. The second direction, where the projection counts nothing because the porcelain parse is broken, is planted separately (`gp-B`).
- **NM-11 · `S1P7-R3-3-BORROW` re-executed at the pin** (`prove-runs/S1P7-R3-3-BORROW.txt`, `--no-record`; its un-fix is `if (false && expect !== undefined)`, which disables the borrow branch): `plant-applied=YES · planted=exit 1 · control=exit 0 · reason=MATCHED`. That is 3 of the 4 gate-plants-backed proofs re-executed clean. The 4th, `S1P7-57-2B-VACUOUS-CONJUNCT`, is re-measured in `gp-A` (NM-8).
- **NM-12 · every non-Playwright proof L4 owns, re-executed one id at a time at the pin** (`class5-reaudit-probes/L4/prove_loop.sh` -> `prove-runs/`, `--no-record`). Set: the 21 non-Playwright entries the range added, plus the 12 other entries whose un-fix is anchored in an L4 file. **32 of 33 `reason=MATCHED` with `plant-applied=YES` and `control=exit 0`** (S1P7-57-2D-DEBT-SPREAD-LEDGER, S1P7-57-2A-PROJECTED-STALENESS, S1P7-R3-3-BORROW, S1P4-C4-3-BOTHDIRECTIONS, S1P4-C4-4-POPULATION, S1P3-D3-CAPS, S1P3-G-LIVENESSLEDGER, S1P5-D5-7-EXPECTREQUIRED, S1P5-D5-13-DERIVEDPOP, S1P5-D5-9-CAPWRAP, S1P6-D1-8-GATECHAIN, S1P6-D2-1-PROOFSTALE, S1P7-U7-VERDICT-MARK, S1P7-U11-WELDED-TOKEN, S1P7-C3-5-LIVEACTIVITY-UPDATE-LANDED, S1P7-C3-5-LIVEACTIVITY-START-LANDED, S1P7-C3-6-LIVEACTIVITY-END-LANDED, S1P7-C3-1-SIRI-DEBTSJSON-UNREAD, S1P7-C3-2-SIRI-PREMIUM-UPSELL, S1P7-D2-12-SIRI-NO-PLAN-UPSELL, S1P7-B1-1-SAVEFORIT-PACE-KEPT, S1P7-C3-14-GOALS-HERO-COUNT, S1P7-C3-2-SIRI-SWIFT-PREMIUM-FALLBACK, S1P7-57-2B-CLAIM-LATTICE, S1P7-57-2C-LOST-FIELD-NAMED, S1P7-57-3-WIDGET-DIRECTION, S1P7-57-4A2-REPLAY-ONCE, S1P7-57-4A2-CARRY-REPLACEMENTS, S1P6-C3-6-DATED-TAP, S1P6-C3-6-HANDLED-NO-REFUSAL, S1P6-C3-6-BUTTON-NAMES-PAYDAY, S1P7-B1-2-OFFER-ASKS-ENGINE). Not scored: S1P7-57-2B-VACUOUS-CONJUNCT (the environmental crash in NM-8, re-measured in `gp-A`). `S1P5-D5-7-EXPECTREQUIRED` runs `typecheck:scripts` (the scripts project only, not the monorepo) and finished under the 1536 MB heap: planted exit 2, control 0. **Registry after the loop: `cmp` IDENTICAL** to the copy taken before it; worktree clean. The 7 Playwright-backed added entries are L3's and were not run.
- **NM-13 · R4-1's record token is not hollow alone** (`plant-runs/r41-delete-freed-assert.0.txt`). `S1P7-R4-1-ONETIMELUMP`'s token `'one-time': 0,` would survive deleting the `freed` assertion that uses the record (the declaration-outlives-use shape the `D3-3` check cannot see in a Record literal). Deleting that `eq(...freed...)` block reds `lint:finding-guards` **1** on `S1P7-R3-4-FREEDPERMONTH — the guard is gone`: the two entries guard the same assertion jointly, and a deletion is caught.
- **NM-14 · class 4's tail, `R5-1`: CLOSED by planting its OWN defect shape**, not only the registry's un-fix. `S1P7-R4-1-ONETIMELUMP`'s recorded un-fix removes the one-time exclusion entirely (the R4-1 shape). `r51-shape-bnpl-only` plants R5-1's actual defect instead: the exclusion keyed back on `type === 'bnpl' && recurrence === 'one-time'` (`payoffCelebration.ts`). `test:app` exits **1** on `R3-4 · debt · one-time — the beat states the money freed per MONTH, $0 (got 50, expected 0)`. That is the `type:'debt'` one-time row, and it carries the entry's `expect` `· one-time`. The derived `Record<Recurrence, number>` × 2-type population reaches the case R4-1's single row missed. Restore byte-verified. Class-4 tail origin; nothing to file.
- **NM-15 · `runAppTests.ts` wiring is seen:** `rc-see-liveactivity-unwired` (the range's one new `import(...liveActivitySync.test)` line removed) makes `lint:runner-completeness` exit **1**, naming `apps/rn/src/liveActivity/liveActivitySync.test.ts`.
- **NM-16 · the new cap is a seen literal:** `cap-see-debt-spread-derived` (`MAX_DEBT_SPREAD_SITES = Object.keys(DEBT_SPREAD_OPEN).length + 4`) makes `lint:cap-literals` exit **1**: `MAX_DEBT_SPREAD_SITES is "..." not a literal`. The `MIN_CAPS` 29->30 raise counts a cap the gate actually reads.
- **NM-17 · the debt-spread ledger's exactness is LIVE** (control for F5/F6): `spread-exact-control` (a second `{ ...d }` appended to `sandboxScenarios.ts`) makes `lint:trust-claims` exit **1**: `sandboxScenarios.ts is ledgered at 1 spread(s) and holds 2`. The vacuous scan sees the REVERSED order: `vac-reversed-control` (`mayClaim(s,'row-figures') && mayClaim(s,'required-plan')` in `drift.ts`) exits **1** with `[vacuous-conjunct] apps/rn/src/store/drift.ts:71`.
- **NM-18 · `gp-A`: one planted `test:gate-plants` run carrying the registry's own recorded un-fixes for `S1P7-57-2A`, `-2D`, `-2B-VACUOUS-CONJUNCT` and `S1P7-R3-3-BORROW` together** (`plant-runs/gp-A-registry-unfixes.0.txt`, 541 s; clean baseline 30 of 30 green in 538 s). **Trust-claims half, VALID:** `[debt-spread]` and `[vacuous-conjunct]` each score `planted=exit 1 · control=exit 0 · reason=WRONG`, the expected failure under their un-fixes. **`S1P7-57-2B-VACUOUS-CONJUNCT` is therefore re-measured and holds** (its own proof died on the environment, NM-8), and `-2D` is cross-checked. **Finding-guards half, VOID, a probe fault, not a finding:** every `lint:finding-guards [...]` scenario shows `control=exit 1`. In a combined plant the un-fixes stay on disk for the whole run, and R3-3's un-fix rewrites `if (expect !== undefined && !e.token.includes(expect)) {`, which IS `S1P7-R3-3-BORROW`'s token. So every real-registry control reds on "the guard is gone". `prove:guards` never hits this (it restores before its control), and those entries already scored MATCHED one at a time (NM-10, NM-11).
- **NM-19 · why both combined runs' finding-guards controls redded (measured, not assumed):** registry un-fix anchors were searched against every line the two runs edited. `gp-A` rewrote the anchors of `S1P7-R3-3-BORROW` and `S1P7-57-2A-PROJECTED-STALENESS` themselves. `gp-B`'s survivor edit rewrote `if (covers(b, a) || covers(a, b)) {`, the anchor of `S1P7-57-2B-VACUOUS-CONJUNCT`. Either way a registered proof's anchor matches 0x, the real-registry control reports it VOID, and every `lint:finding-guards [...]` scenario scores `control=exit 1`. So `gp-B`'s `[R5-2-boundary]` / `[projected]` rows say nothing, and both are measured directly instead (NM-20). ⚠️ Lesson for the harness user, not a defect: a plant into a gate FILE that carries registry anchors voids the gate-plants controls of every scenario that reads that registry.
- **NM-20 · class 4's tail `R5-2`, and `--projected`'s second direction, measured directly** (`direct_2x2.py` -> `direct_2x2.out.txt`, `direct-runs/`). These are fixture registries only, so no real-registry control is involved (see NM-19). Every fixture run exits 1 on `MIN_ENTRIES`, so only the named line discriminates.
  - **R5-2:** with the pinned word-boundary matcher, a note naming the longer sibling `A3-14` still PRINTS the NEIGHBOUR refusal, and a note naming the lender `A3-1` legitimately waives it. With the substring revert, the `A3-14` note waives silently (refusal absent). **Closed by planting, both directions.** Class-4 tail; nothing to file (its owed registry row is already filed, NM-4).
  - **`--projected`:** at the pin, a dirty tracked target PRINTS `as if committed` with all 9 fixture proofs projected, and the clean control prints nothing. With the porcelain parse broken (`line.slice(4)`), the dirty target projects 0 and the refusal is absent. The parse direction is live; the `[projected]` scenario plants the same shape, so it would score WRONG under this edit (inferred from identical fixtures, not separately run). Restores byte-verified, no fixtures left, tree clean.

## Derived class-5 membership and disagreements

Four sources, and they do not agree:

- **`CLASSIFICATION.md` §CLASS 5 table (13):** C3-13 C3-8 C3-9 C3-11 C3-5 C3-1 C1-1 C1-5 C1-6 D2-12 C3-2 B1-1 C3-14.
- **Plan `.12.6.5` (`DEBT_ELEVATION_PLAN.md:82-92`):** the 13, plus `C1-2` *(pulled forward)* · `C1-3` *(swept inline)* · `C3-6` *(pass 7)* · pass-6 `C3-6` *(reopened at .5.7 ④a)* · `B1-2` *(from class 6, ④b)* · `.5.7`'s instrument items.
- **Commit messages in the range:** all of the above, plus many cited-only ids (A1-7, A3-x, C4-x, C5-3, D1-8, D3-x, D5-x -- context references, not closures) and `R5-1`/`R5-2` (class 4's tail).
- **Registry entries the range ADDED (28):** by id they name C3-5 (2) · pass-7 C3-6 (1) · C3-1 · C3-2 (2, incl. the Swift fallback) · D2-12 · B1-1 · C3-14 · C3-8 (the free-user e2e twin only) · B1-2 · pass-6 C3-6 (3) · plus 11 instrument/render entries (`S1P7-57-*`).

**Disagreement (measured, probe: registry `what`/id text searched for each id with a word boundary):**
- 6 table closures (`C3-13` `C3-9` `C3-11` `C1-1` `C1-5` `C1-6`) and the plan's `C1-2` `C1-3` have **no registry entry naming them as a pass-7 closure.**
- Hits for C1-1 / C1-6 / B1-1 are pass-6 or pass-4 ids (`S1P6-C1-1-NAMETHEFIGURE`, `S1P6-C1-6-AMOUNTCOLLAPSE`, `S1P4-C4-7-SURFACES`): the pass-6/pass-7 collision the brief warns of.
- C3-13 is named only by the `[debt-spread]` instrument entry; C3-11 only by the lattice/vacuous instrument entries. Neither is a proof of the closure itself.
- Unit/e2e assertions labelled with the id exist for `C3-13` `C3-9` `C1-2` `C1-3` (`trustSelectors.test.ts`, `journeySelectors.test.ts`, `dataRepairsCopy.test.ts`). The plan records `C3-11` "e2e planted three ways" and `.5.4d` "plants Q1-Q6 unit, R1-R4 e2e". So those closures were planted when fixed, but they carry no standing registry row. See F-registry below.
- The plan says `.5.1` "re-derived all 13 (five corrections)"; the table still has 13 rows. Consistent.

## Files opened

Read in full or in the ranges named, at the pin worktree unless marked:
- `CLASS5-REAUDIT-BRIEF.md`, `../2026-08-29-s1-money-pass5/RESUME-PROTOCOL.md`, `CLASS5-REAUDIT-LANES.tsv` (L4 rows) -- main checkout
- `scripts/run-gates.ts` (full) · `scripts/check-finding-guards.ts` (full) · `scripts/check-cap-literals.ts` (full)
- `scripts/check-trust-claims.ts` (1-120, the debt-spread and vacuous-conjunct blocks, 715-775; base `:466-637` via `git show c7df99c2`)
- `scripts/test-gate-plants.ts` (Scenario interface 46-110, the four range scenarios, run loop 787-914)
- `scripts/prove-guards.ts` (120-900) · `scripts/lib/verdict.ts` (1-170) · `scripts/lib/joinedCode.ts` (`normaliseFragment`)
- `scripts/finding-guards.json` (via probes: added / changed / anchored entries; 5 entries printed in full)
- `apps/rn/src/testing/runAppTests.ts` (range diff) · `scripts/surface-coverage.s1.json` (range diff)
- `apps/rn/src/store/affordability.test.ts` (120-176) · `apps/rn/src/store/payoffCelebration.test.ts` (180-241) · `apps/rn/src/widget/widgetSync.test.ts` (range diff) · `apps/rn/src/liveActivity/liveActivitySync.test.ts` (default export)
- `scripts/check-audit-closure.ts` (sources only) · `scripts/check-runner-completeness.ts` (run-gates references) · `.github/workflows/web-e2e.yml` (80-100)
- Commits: `2df9ece8` (payoffCelebration.ts), `55fcc88f`, `b8cc6266`, `48aa6697`, `d559e4dd`, `49fdf5ce` (stat/message)
- `docs/audits/2026-09-02-s1-money-pass7/CLASSIFICATION.md` §CLASS 5 · `docs/DEBT_ELEVATION_PLAN.md:57,82-175` · `docs/DEBT_ELEVATION_LOG.md:32465-32495` + grep hits · `docs/DEBT_ELEVATION_BACKLOG.md` grep hits · `CLASS4-REAUDIT-5.md` R5-1/R5-2 headings

## Summary

| severity | attributable | reservoir | class4-tail |
|---|---|---|---|
| blocker | 0 | 0 | 0 |
| major | 0 | 0 | 0 |
| minor | 6 (F1-F6) | 1 (F5b) | 0 |

- **Closures re-proven by planting at the pin:** 32 of 33 non-Playwright proofs L4 owns scored `MATCHED` one at a time (NM-12). The 33rd (`S1P7-57-2B-VACUOUS-CONJUNCT`) died on the environment and was re-measured in `gp-A` (NM-18). Class 4's tail: `R5-1` is closed against its own defect shape (NM-14); `R5-2` is closed in both directions (NM-20). Registry `cmp` IDENTICAL after every proof run; every plant's restore byte-verified.
- **What class 5's instruments broke:** nothing measured as broken. Each finding is a claim a new instrument makes that its standing proof does not hold it to: F3 the D81 refusal, F4 a proof whose red is not its token, F5 a ceiling that does not pin, F6 one-direction scans. Plus a record gap (F2) and a stale derivation (F1).
- ⚠️ `authored` is 9 of cap 9 at the pin (NM-1): every remedy above that adds a registry proof re-enters the record deadlock.
- **Not run:** the 7 Playwright-backed registry entries the range added (L3's). No whole-monorepo typecheck; no OOM occurred. The base worktree `audit-c5r1-L4-base` was removed (junctions first); the primary worktree `audit-c5r1-L4` is left clean for the driver.
