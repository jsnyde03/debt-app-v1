exit=1
⛔ finding-guards: 2 of 2 findings carry a standing guard; 0 unguarded (cap 1, downward-only).
   proof: 0 EXECUTED · 0 of them STALE (cap 8) · 1 authored but never run (cap 9) · 0 guard-only (cap 0) · 1 never tested (cap 119)
          ⛔ "authored" is a plan to measure, not a measurement — `npm run prove:guards -- --id=<ID>` records it.
          ⛔ "STALE" means the target moved since the proof was measured — re-run it; 2 of 32 did not hold.

❌ finding-guards: 4 problem(s).

  • PLANT-L4-BORROWER — its proof's `expect` is not in its own token, and IS in S1-CLASS4-A3-1.
        "only the lender may survive"
        That is a red on a NEIGHBOUR'S assertion: the plant fires, the sibling fails, and this
        entry is recorded proven without its own assertion ever being reached. Either point
        `expect` at this entry's own assertion, or NAME the lender in `proofNote` and say why
        this finding has no assertion of its own. A note that does not name it does not waive it.
  • the registry holds 2 findings; 330 are expected. Entries were REMOVED — a finding dropping out is how a closure stops being tracked. Do not lower the floor.
  • 0 findings are unguarded and the cap is still 1. Lower it to 0 — the cap is the high-water mark, and leaving it above the count is room for a guard to disappear unnoticed.
  • 1 findings are unproven — nobody has ever made their guard red and the cap is still 119. Lower it to 1 in the same edit — a cap above its own count is slack the next un-evidenced entry hides in.

  [D67]: a closed finding needs a standing guard, or it is not closed.

