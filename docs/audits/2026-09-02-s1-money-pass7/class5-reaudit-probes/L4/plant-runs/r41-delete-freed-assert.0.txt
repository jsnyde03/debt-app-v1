$ npm run -s lint:finding-guards
exit=1 secs=37

⛔ finding-guards: 328 of 330 findings carry a standing guard; 1 unguarded (cap 1, downward-only).
   proof: 200 EXECUTED · 2 of them STALE (cap 8) · 9 authored but never run (cap 9) · 0 guard-only (cap 0) · 119 never tested (cap 119)
          ⛔ "authored" is a plan to measure, not a measurement — `npm run prove:guards -- --id=<ID>` records it.
          ⛔ "STALE" means the target moved since the proof was measured — re-run it; 2 of 32 did not hold.
     stale: S1-ROUTE-STALE-READ — scripts/audit-route.ts has moved since f7e39483 (measured 2026-08-31)
     stale: S1-ROUTE-EXIT-REACHABLE — scripts/audit-route.ts has moved since f7e39483 (measured 2026-08-31)
     unguarded: GAP-14 — S0.13 GAP-14 — the open question is now ANSWERED, and the answer is a deliberate non-fix (S1.10.6.5.8.3). Measured rather than assumed: (1) lint:gate-freshness legitimately reds MID-AUDIT by [D74]'s own design, so gating every push would red the lane for weeks - and web-e2e.yml's header documents that exact failure killing the previous lane ('a permanently-red gate is worse than no gate - it trains you to ignore the one signal that is supposed to mean something'); (2) gate:record is deliberately NOT in CI, so there is no post-record moment to hook; (3) a freshness check run straight after gate:record is tautological - the record was just written from that tree; (4) nothing in CI CONSUMES gate-status.json, so there is no reader to protect. ⛔ What the measurement DID surface is a real unguarded claim in the same workflow, now closed by lint:ci-chain (S1P3-CI-CHAIN). Re-open this only with a named CI consumer of the record

❌ finding-guards: 1 problem(s).

  • S1P7-R3-4-FREEDPERMONTH — the guard is gone from apps/rn/src/store/payoffCelebration.test.ts: no "the beat states the money freed per MONTH"  (class 4 round-3 R3-4 - payoffCelebration stamped freed: subject.minimumPayment, a per-INSTALLMENT figure, and the beat rendered it as 'Freed $50/mo' where a weekly debt frees $216.67 - on screen, spoken, and on a ShareCard, understating the user's own win by 4.33x on the product's payoff moment. bnplMonthlyEquivalentMinimum is the declared producer of cost per month and its four call sites are all projection engines, so a celebration was never in the population anyone enumerated: the SIXTH consecutive undercount of that site list. cyclesPerMonth is REQUIRED per A5-1, so all 18 call sites became typecheck errors rather than silent monthly assumptions.)
        the file survived; the assertion inside it did not, which is the shape this gate exists for

  [D67]: a closed finding needs a standing guard, or it is not closed.

