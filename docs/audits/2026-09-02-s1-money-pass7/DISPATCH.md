# Pass 7 — the route, verified. ⛔ NOT YET DISPATCHED.

> ## ▶ START HERE — the dispatching session
>
> 🎯 **2026-09-02: the shape is AGREED. Nothing below needs re-deciding; it needs verifying against the
> tree and executing.** This is the pass-6 precedent — route in the planning session, **run in a fresh
> one** — because a session that has spent itself deciding *what* to dispatch is the wrong one to also
> hold the dispatch.
>
> | | |
> |---|---|
> | **Lanes** | **12**, generated by `audit:sublanes` — `A1 A2 A3 B1 B2 B3 C1 C2 C3 D1 D2 D3` |
> | **Population** | **640 files · 106.0k lines · 0 unrouted · 0 owed** |
> | **Exit** | `npm run audit:read-coverage -- --surface=s1 --pass=s1p7` at **457/457** — all 457 exit-bearing files are already in a sub-lane |
> | **Read tracking** | every lane emits `READ-<lane>.txt` **incrementally**, as it opens files |
> | **Constraints** | no sub-agents inside lanes · heap 1536 MB · **an OOM is a FINDING, never a retry** · incremental writes |
> | **Quoted worst case** | **~3–5M tokens.** Estimated from line counts, not measured spend — pass 6 was the same shape at 106k lines |
>
> ### ⚠️ Verify before acting — this record is a hypothesis about a tree that has moved
>
> ```bash
> npm run lint:rn                 # 49 gates — S2/S3/S4's coverage gates are new this round
> npm run audit:route-check       # re-asserts 0 owed  ⚠️ SEE THE PIN NOTE BELOW
> git log --oneline -1            # confirm the pin below is still pass 6's target tree
> ```
>
> ⛔ **`audit:route-check` in `package.json` is still pinned to PASS 6's arguments**
> (`--since=65566a09 --unread-pass=s1p5 --exit-pass=s1p6`). Re-point it to this round's, or run the
> explicit command below and read its own summary line. **Do not read the pipeline's status** — the
> harness has reported exit 0 over a red gate.
>
> ```bash
> npx tsx scripts/audit-route.ts --surface=s1 --since=4c0f7689 \
>   --unread-pass=s1p6 --exit-pass=s1p7 --out=docs/audits/2026-09-02-s1-money-pass7 --check
> ```
>
> ### ⛔ The 12-way split is a spec of SUBJECTS, not a load partition — to check, not to adopt unread
>
> | lane | files | lines | exit-bearing | subject |
> |---|---|---|---|---|
> | **A1** | 68 | 9.7k | 51 | The spec tree — every test that CLAIMS to guard the engine |
> | **A2** | 55 | 6.4k | 55 | The debt engine itself — payoff, amortisation, ids, balances |
> | **A3** | 79 | 10.7k | 65 | The rest of the engine — cashflow, forecast, payCycle, guardian, income, recurrence |
> | **B1** | 40 | 7.3k | 38 | The selectors — what the stored number is turned into before a screen sees it |
> | **B2** | 51 | 9.1k | 35 | The store core — how the number is written, migrated, remembered and re-read |
> | **B3** | 64 | 9.2k | 36 | Storage, backup, formatting and dates — how the number is spelled and persisted |
> | **C1** | 52 | 7.8k | 43 | The plan cards — the densest place a true number becomes a false sentence |
> | **C2** | 72 | 8.2k | 66 | The rest of the components — payoff, progress, entities, onboarding, more, UI primitives |
> | **C3** | 75 | 9.6k | 68 | Routes and the surfaces OUTSIDE the app — widget, Siri, Lock Screen, notifications, hooks, theme |
> | **D1** | 31 | 8.4k | 0 | The gates and the suites — every instrument whose job is to report the tree green |
> | **D2** | 41 | 13.9k | 0 | The proof machinery and the config no surface owns |
> | **D3** | 12 | 5.4k | 0 | The **LEGACY NEXT ROOT** — deleted by P6.11. **Reduced mandate; see the brief** |
>
> ⚡ **`D2` carries the most lines on few files** (13.9k over 41) because the instruments carry enormous
> docblocks. Sizing by file count alone under-weights it by a factor of three.
>
> ### ⛔ Hand every lane `BRIEF.md`, and do not put your own verdict in it
>
> The brief carries the finding vocabulary, the origin table, the severity form and the constraints. It
> deliberately does **not** contain answers. `audits-always-use-fresh-agents`: the session that wrote or
> fixed the code carries its premises and re-reads its own reasoning instead of the code.

**Route generated:** 2026-09-02 · **Target tree:** `c823e3ee` (`v1.7-dev`) *(plus this round's own commit)*.
**Pin:** `4c0f7689` → HEAD — pass 6's target tree.
**Followed pass:** `s1p6`, via `--unread-pass=s1p6`. **Dispatched for:** `s1p7`, via `--exit-pass=s1p7`.

⛔ **This file records the ROUTE, not a dispatch.**

---

## The origin split

```
640 routed · 0 missing on disk · every CHANGED tracked file since 4c0f7689 accounted for
  (55 excluded as prose or binary)
11 first-look · 99 fix-churn · 46 instrument · 31 off-surface · 77 neighbour
  · 26 s0-first-look · 350 stale-read
surface: 495 files (11 unswept) · S0: 130 files
⭐ exit reachable: 457 money-bearing file(s) still owed to s1p7, every one in a lane (0 already read).
```

⚡ **`stale-read` is 350 — more than half the route.** These are money-bearing files **pass 6 did not
read**. Pass 6 hit 446/446 on its *own* exit population; this is the absolute question asked again for
`s1p7`, and every file starts owed.

⚡ **`fix-churn` is 99 and `instrument` is 46.** That is where this round's repairs are — twelve triage
classes, two new lint gates, 14 new guards and two repairs to the proof harness itself. **Every prior
round found the previous round's fixes defective.**

## ⛔ Two known holes, recorded so the dispatch does not rediscover them

1. **9 files** sit in the import neighbourhood of a never-swept file and reached **no lane** — the
   neighbourhood is seeded from *changed* only. [D5-8].
2. **26 routed files are owned by no claims file**, listed in `UNSEEN-NEIGHBOURS.txt`. ⚠️ **This was 87
   before this round.** `S1.13.7.12.1` built S2/S3/S4's claims files and `audit-route.ts` was still asking
   the old question; pointing it at all five took it to 26. The remainder is the legacy root (12), Swift
   sources and `apps/rn/modules` (5), the manifests and this gate's own records (7), and `.gitignore` /
   `package.json` — all correctly homeless, being outside `SOURCE_EXT` or `commonExcluded` by name.

## ⚠️ What changed in the instruments since pass 6 — verify, do not assume

- **`lint:rn` is 49 gates**, not 46 — `lint:s2-coverage`, `lint:s3-coverage`, `lint:s4-coverage` are new.
- **S2/S3/S4 exist as DERIVED surfaces**: they receive what S0/S1 route to them rather than walking roots.
  S3 = 39 files, S4 = 46, **S2 = 0** *(its one member, `date-field.spec.ts`, was returned to S1 — it is the
  only e2e guard for a control S1 owns, and it sat on a surface that does not open until after S1
  converges)*.
- **`prove:guards` retries a killed web server** ([D78]) up to 6 times and **faults** rather than scoring
  when it never comes up. Every retry prints. ⚠️ If you see `re-attempting (n/6)`, that is the
  environmental kill, not your guard.
- **`prove:guards --selftest` is 5 cases**, not 3.
